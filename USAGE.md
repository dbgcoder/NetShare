# NetShare 使用说明

NetShare 是一款基于 Qt6 的局域网文件共享工具，支持文件/文件夹分享、分块传输、断点续传、拖放上传和 Web 端访问。

---

## 1. 安装与启动

### 环境要求
- Windows 10 或更高版本
- 无需安装 Qt 或其他运行时（绿色版已包含所有依赖）

### 启动方式
1. 解压 `netshare-v1.0.zip` 到任意目录
2. 双击运行 `NetShare.exe`
3. 首次启动会弹出注册/试用页面，选择试用即可开始使用
4. 程序启动后最小化到系统托盘，双击托盘图标可打开主界面

---

## 2. 界面导航

主界面采用左侧导航栏设计，包含以下页面：

| 页面 | 说明 |
|------|------|
| 发送 | 创建和管理文件分享 |
| 接收 | 查看接收的文件 |
| 传输 | 查看传输进度和历史 |
| LAN | 局域网设备发现 |
| 设置 | 通用、网络、安全等配置 |

标题栏右侧功能按钮：
- 💬 消息 - 局域网即时聊天
- ⚙ 设置 - 打开设置页面
- 最小化/最大化/关闭

---

## 3. 分享文件

### 方法一：通过界面创建分享

1. 点击左侧 **发送** 页面
2. 点击 **新建分享** 按钮
3. 选择要分享的 **文件** 或 **文件夹**
4. 设置有效期（24小时 / 7天 / 30天 / 永久）
5. 可选：设置访问密码
6. 点击 **确定** 完成创建

### 方法二：拖放文件（快捷方式）

直接将文件或文件夹 **拖放到 NetShare 窗口**，软件会自动创建 24 小时有效的分享链接。

### 分享链接格式
```
http://[局域网IP]:8080/s/[token]
```

### 分享管理功能
- **复制链接** - 一键复制分享链接到剪贴板
- **二维码** - 生成分享链接的二维码，方便手机扫码访问
- **取消分享** - 提前终止分享
- **筛选** - 按 全部/活跃/已过期 筛选分享

---

## 4. 接收文件

### 浏览器访问
其他设备（手机、电脑、平板）通过浏览器访问分享链接即可：
- 浏览文件目录
- 下载文件/文件夹
- 支持断点续传

### 从浏览器上传到电脑
如果设置中开启了 **允许上传**，浏览器端可以：
1. 打开分享页面
2. 选择文件上传到电脑

---

## 5. 传输管理

**传输** 页面显示：
- 当前传输任务的进度
- 传输速度
- 支持暂停/继续传输

### 分块传输特性
- 大文件自动分块，并发传输提高速度
- 支持断点续传，中断后从断点继续

---

## 6. 局域网设备发现 (LAN)

**LAN** 页面自动扫描局域网内运行 NetShare 的设备：
- 显示设备名称和 IP 地址
- 点击设备可直接与其建立聊天或文件传输

---

## 7. 即时聊天

点击标题栏 💬 按钮进入聊天功能：
- 局域网内设备间发送文字消息
- 支持多设备聊天
- 未读消息数量提示

---

## 8. 设置说明

### 通用设置
| 选项 | 说明 | 默认值 |
|------|------|--------|
| 设备名称 | 局域网中显示的名称 | NetShare-PC |
| 主题 | 界面主题（深色/浅色） | 深色 |
| 语言 | 界面语言（中文/英文） | 中文 |
| 开机自启 | 开机自动启动 | 关闭 |
| 最小化到托盘 | 关闭窗口时最小化到托盘 | 开启 |
| 显示通知 | 接收文件等操作时弹出通知 | 开启 |

### 网络设置
| 选项 | 说明 | 默认值 |
|------|------|--------|
| 服务端口 | HTTP 服务端口 | 8080 |
| 最大连接数 | 同时允许的连接数 | 10 |
| 带宽限制 | 上传速度限制（KB/s），0=不限 | 0 |
| 自动检测IP | 自动获取局域网IP | 开启 |
| 手动IP | 自定义绑定IP地址 | 空 |

### 安全设置
| 选项 | 说明 | 默认值 |
|------|------|--------|
| 访问密码 | 是否需要密码才能访问分享 | 关闭 |
| 密码 | 访问密码内容 | 空 |
| 允许上传 | 是否允许浏览器端上传文件 | 开启 |
| 允许删除 | 是否允许浏览器端删除文件 | 关闭 |
| 访问日志 | 记录文件访问日志 | 开启 |

---

## 9. 常见问题

### Q: 手机无法访问分享链接？
- 确保手机和电脑连接同一 WiFi/局域网
- 检查 Windows 防火墙是否放行 NetShare（首次启动会自动添加）
- 确认电脑的局域网 IP 地址正确（状态栏显示）

### Q: 文件传输中断怎么办？
- NetShare 支持断点续传，重新下载会从断点继续
- 不会重复下载已完成的部分

### Q: 如何修改默认端口？
在设置 → 网络 → 服务端口修改，修改后需要重启应用。

### Q: 分享链接有有效期吗？
创建分享时可选择有效期，过期后链接自动失效。也可手动取消分享。

### Q: 支持分享文件夹吗？
支持。文件夹会自动打包，对方可以浏览并下载整个文件夹。

---

## 10. 联系方式

如有问题或建议，请联系：HD5080@outlook.com

---

*NetShare v1.0 - 局域网文件共享工具*
