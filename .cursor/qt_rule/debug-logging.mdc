---
description: Qt Creator 通用调试与日志规则；规定 Application Output 输出、日志格式、脱敏、错误定位和回报要求
globs:
  - "**/*.cpp"
  - "**/*.h"
  - "**/*.hpp"
  - "**/*.qml"
  - "**/*.cmake"
  - "**/CMakeLists.txt"
alwaysApply: false
---

# Qt Creator 调试规则

## 1. 使用时机

当需要执行以下任务时，必须使用本规则：

- 添加调试日志；
- 定位崩溃、卡顿、无响应、加载失败；
- 排查 QML 页面、组件、Loader、ListView、Model 问题；
- 排查 C++ 业务链路、数据库、网络、文件读写问题；
- 排查编译通过但运行行为异常的问题；
- 用户要求“看控制台日志”“加调试信息”“定位问题”“分析耗时”。

---

## 2. 输出位置

调试信息统一输出到 **Qt Creator → Application Output**。

要求：

- QML 使用 `console.log()` / `console.warn()` / `console.error()`；
- C++ 默认使用 `qWarning().noquote()`；
- 只有明确开启 verbose 时，才使用 `qInfo()` / `qDebug()`；
- 禁止只写本地日志文件而不输出到 Application Output；
- 禁止在软件界面内新增调试窗口、调试面板、日志 TextArea、ScrollView 等 UI。

调试信息只给开发者看，不进入正式用户界面。

---

## 3. 日志级别

### 3.1 默认可见日志

默认使用：

```cpp
qWarning().noquote() << message;
```

适用于：

- 关键链路开始 / 结束；
- 错误原因；
- 异常分支；
- 数据为空；
- 文件不存在；
- QML 加载失败；
- 数据库读写失败；
- 网络请求失败；
- 用户正在排查的问题链路。

### 3.2 verbose 日志

大量、频繁、循环内样本日志，必须使用 verbose 开关控制。

推荐环境变量：

```text
AIMODEL_VERBOSE_LOG=1
```

可接受值：

```text
1 / true / yes / on
```

verbose 适用于：

- 大量列表逐行输出；
- 高频刷新日志；
- 循环样本；
- 大 JSON 摘要；
- 性能耗时细节；
- 临时排查日志。

---

## 4. 日志格式

统一格式：

```text
[ISO时间][模块名][tag] message
```

示例：

```text
[2026-07-03T12:30:01.123Z][PageLoader][error] source=pages/HomePage.qml status=3 err=Xxx is not a type
[2026-07-03T12:30:01.456Z][TaskService][load.begin] projectId=abc
[2026-07-03T12:30:01.789Z][TaskService][load.end] count=25 costMs=18
```

要求：

- 模块名必须稳定，便于过滤；
- tag 必须表达链路阶段；
- 同一问题链路的 tag 必须前后一致；
- 时间优先使用 UTC ISO；
- QML 暂时取不到 ISO 时间时，可使用 `[*]`。

---

## 5. 必打日志点

排查链路问题时，至少包含以下阶段：

```text
begin
input
before
after
success
fail
end
```

常用 tag 示例：

```text
load.begin
load.input
load.db
load.success
load.fail
load.end
save.begin
save.input
save.db
save.success
save.fail
save.end
request.begin
request.payload
request.success
request.fail
response.parse
```

要求：

- 开始和结束必须成对出现；
- 失败日志必须包含失败原因；
- 数据为空必须明确打印为空，而不是静默；
- 异步链路必须打印触发点、回调点、结果点。

---

## 6. QML 调试规则

QML 调试优先使用：

```qml
console.warn("[*][Module][tag] key=" + value)
```

适用场景：

- 页面加载；
- Loader 状态；
- Model 数据；
- ListView / Repeater 显示异常；
- 信号是否触发；
- 属性绑定结果；
- 用户点击、拖放、选择等交互事件。

### 6.1 Loader.Error 必打字段

当出现 `Loader.Error` 或页面加载失败时，必须输出：

- `source`
- `status`
- `active`
- `visible`
- `isCurrent`
- `errorString`

推荐格式：

```qml
console.error("[*][PageLoader][error] source=" + loader.source
              + " active=" + loader.active
              + " visible=" + loader.visible
              + " status=" + loader.status
              + " err=" + (loader.errorString() || "(empty errorString)"))
```

若 `errorString` 为空，优先检查：

1. QML 类型未注册；
2. import 路径错误；
3. `qt_add_qml_module` 未包含新增 QML 文件；
4. 资源路径错误；
5. Loader 条件导致组件未真正加载。

---

## 7. C++ 调试规则

C++ 链路默认使用：

```cpp
qWarning().noquote() << QString("[%1][Module][tag] key=%2")
    .arg(QDateTime::currentDateTimeUtc().toString(Qt::ISODateWithMs))
    .arg(value);
```

适用场景：

- 服务入口；
- 参数检查；
- 文件读写；
- 数据库查询；
- SQL 执行失败；
- 网络请求；
- JSON 解析；
- 信号发射；
- 异步回调；
- 线程任务开始和结束。

要求：

- 错误日志必须包含 `err` 或 `error` 字段；
- 数据库错误必须包含 SQL 摘要和错误原因；
- 文件错误必须包含路径和存在性检查；
- 网络错误必须包含状态码、错误码和响应摘要；
- JSON 错误必须包含解析错误和字段缺失情况。

---

## 8. 脱敏规则

禁止在 Application Output 输出敏感明文。

必须脱敏的字段：

- token
- sessionToken
- API Key
- password
- authorization
- cookie
- secret
- private key
- access key

推荐输出：

```text
token=<redacted len=128>
apiKey=<redacted len=64>
password=<redacted>
```

允许输出：

- 字符串长度；
- 是否为空；
- 前后极短摘要，但不得泄露可用密钥；
- JSON 字段结构。

---

## 9. 性能耗时规则

排查卡顿或慢操作时，必须输出耗时。

推荐 tag：

```text
perf.begin
perf.step
perf.end
```

推荐字段：

- `costMs`
- `step`
- `count`
- `thread`
- `async`

示例：

```text
[2026-07-03T12:30:01.123Z][ImageLoader][perf.begin] count=100
[2026-07-03T12:30:01.456Z][ImageLoader][perf.step] step=db costMs=12
[2026-07-03T12:30:01.789Z][ImageLoader][perf.end] totalMs=86
```

---

## 10. 常见问题排查顺序

### 10.1 页面加载失败

按顺序检查：

1. Application Output 中的 QML error；
2. Loader `source/status/errorString`；
3. import 路径；
4. QML 类型是否注册；
5. `qt_add_qml_module` 是否包含文件；
6. 资源路径是否正确。

### 10.2 数据不显示

按顺序检查：

1. 数据请求是否触发；
2. 数据源是否返回；
3. Model 是否写入；
4. View 是否绑定正确；
5. delegate 是否收到字段；
6. 是否被筛选、排序、分页过滤掉。

### 10.3 点击无反应

按顺序检查：

1. 点击事件是否触发；
2. 参数是否正确；
3. 信号是否发出；
4. C++ slot 是否收到；
5. 后续回调是否返回；
6. UI 状态是否更新。

### 10.4 数据库异常

按顺序检查：

1. 数据库路径；
2. 表是否存在；
3. SQL 是否执行；
4. 绑定参数是否为空；
5. 错误信息；
6. 查询结果数量。

---

## 11. 执行要求

新增调试时必须做到：

- 先保证 Qt Creator Application Output 可见；
- 再考虑是否补充文件日志；
- 日志 tag 必须能串起完整链路；
- 错误分支必须有日志；
- 空数据分支必须有日志；
- 高频日志必须受 verbose 控制；
- 调试完成后，临时噪声日志应删除或改为 verbose。

---

## 12. 完成后回报

任务完成后，必须向用户说明：

- 日志在哪里查看：Qt Creator → Application Output；
- 本次新增或调整了哪些模块 tag；
- 是否需要设置 verbose 环境变量；
- 是否涉及敏感字段脱敏；
- 下一次复用的规则文件名：`qt_rule/debug-logging.mdc`。

---

## 13. 禁止行为

禁止：

- 在正式 UI 中加入调试窗口或日志面板；
- 只写文件日志，不输出到 Application Output；
- 输出明文 token、API Key、密码等敏感信息；
- 使用杂乱无统一格式的日志；
- 只打印“失败了”但不打印原因；
- 只打印“数据为空”但不打印关键参数；
- 高频循环日志不加 verbose 开关；
- 调试完成后遗留大量无门控刷屏日志；
- 用户要求 Qt Creator 看日志时使用 `qDebug()` 导致默认不可见。
