---
description: Qt/QML 项目结构、命名、分层与性能规则
globs:
  - "**/*.qml"
  - "**/*.cpp"
  - "**/*.h"
  - "**/*.hpp"
  - "**/CMakeLists.txt"
alwaysApply: true
---

# Qt/QML 商用项目架构规则：精简版

## 1. 项目结构规则

项目目录必须职责清晰，并统一采用以下商用项目结构：

```text
MyProject/
├── CMakeLists.txt
│
├── cmake/                         # CMake 模块/脚本
│
├── app/
│   ├── CMakeLists.txt
│   ├── main.cpp
│   └── MainWindow.cpp
│
├── src/
│   ├── CMakeLists.txt
│   ├── core/                       # 核心业务逻辑（与 UI 无关）
│   ├── gui/                        # Widgets、窗口、对话框、视图组件
│   ├── service/                    # 业务服务，比如下载、解析、任务调度
│   ├── models/                     # 数据模型、状态模型、Qt Model/View 模型
│   ├── controller/                 # UI 与业务逻辑之间的协调层
│   ├── infrastructure/             # 日志、配置、数据库、网络、文件系统
│   └── platform/                   # Windows/macOS/Linux 平台差异代码
│
├── qml/
│   ├── Main.qml
│   ├── components/
│   ├── pages/
│   └── themes/
│
├── resources/                      # 图标、图片、字体、QRC 资源
│   ├── icons/
│   ├── images/
│   └── resources.qrc
│
├── translations/
│   ├── MyProject_zh_CN.ts
│   └── MyProject_en_US.ts
│
├── docs/                           # 项目文档
│   ├── architecture.md
│   └── performance.md
│
├── tests/
├── plugins/
├── 3rdparty/
│
├── tools/
│   ├── scripts/
│   └── devtools/
│
└── build/
```

根目录只允许放项目级配置文件和一级功能目录。禁止在根目录随意创建源码、QML、资源或临时文件。

新增文件必须按职责放入对应目录：

- 程序入口放入 `app/`；
- 核心业务逻辑放入 `src/core/`；
- Widgets、窗口、对话框、视图组件放入 `src/gui/`；
- 业务服务放入 `src/service/`；
- 数据模型、状态模型、Qt Model/View 模型放入 `src/models/`；
- UI 与业务逻辑协调层放入 `src/controller/`；
- 日志、配置、数据库、网络、文件系统放入 `src/infrastructure/`；
- 平台差异代码放入 `src/platform/`；
- QML 页面放入 `qml/pages/`；
- QML 复用组件放入 `qml/components/`；
- QML 主题放入 `qml/themes/`；
- 图标、图片、QRC 放入 `resources/`；
- 翻译文件放入 `translations/`；
- 项目文档放入 `docs/`；
- 脚本和开发工具放入 `tools/`。

---

## 2. 命名规则

C++ 类、头文件、源文件使用大驼峰：

```text
UserModel.h
UserModel.cpp
VideoThumbnailGenerator.h
VideoThumbnailGenerator.cpp
```

QML 文件使用大驼峰：

```text
HomePage.qml
SettingsPage.qml
UserDelegate.qml
RoundButton.qml
```

目录使用小写蛇形：

```text
core/
data_models/
video_processing/
image_cache/
```

测试文件使用：

```text
test_user_model.cpp
test_video_thumbnail_generator.cpp
```

C++ 成员变量使用 `m_` 前缀，例如：

```cpp
m_userName
m_isLoading
m_cacheManager
```

---

## 3. 总体原则

项目必须遵循：

**界面 QML 优先，逻辑 C++ 优先。**

QML 负责界面展示、交互、动画和布局；C++ 负责业务逻辑、数据处理、网络、数据库、文件、图像、视频、算法和耗时任务。

---

## 4. UI 规则

所有窗口、页面、控件、布局、动画、手势必须优先使用 QML 实现。

QML 中允许少量界面逻辑，例如表单校验、页面切换、简单状态控制。

QML 中禁止包含：

- 业务规则；
- 数据库操作；
- 文件读写；
- 网络请求；
- 图像处理；
- 视频处理；
- 复杂计算；
- 大型循环；
- 递归算法。

界面颜色、字体、图标、间距、动画等视觉配置必须放在 QML、主题文件或统一样式组件中，禁止硬编码到 C++。

---

## 5. C++ 逻辑规则

业务逻辑、数据处理、模型管理、系统交互和耗时任务必须使用 C++ 实现。

C++ 向 QML 暴露功能时，应使用：

- `Q_PROPERTY` 提供属性绑定；
- `Q_INVOKABLE` 或槽函数供 QML 调用；
- `signals` 通知 QML 状态变化；
- `QAbstractListModel` / `QAbstractTableModel` 提供列表或表格数据。

禁止 C++ 直接操作 QML 控件样式、布局和界面对象。

---

## 6. 通信与线程规则

QML 与 C++ 之间必须通过 Qt 标准机制通信：

- 属性绑定；
- 信号/槽；
- 模型通知；
- 明确的接口类。

任何可能阻塞 UI 的任务必须放到后台线程，例如：

- 数据库查询；
- 文件扫描；
- 网络请求；
- 图片处理；
- 视频处理；
- 大数据计算；
- OpenCV / FFmpeg 处理。

后台任务完成后必须通过信号通知 QML，不得直接跨线程操作 UI。

---

## 7. 大数据性能规则

超过 20 条数据必须使用：

- `ListView`
- `GridView`
- `TableView`

禁止使用 `Repeater` 渲染大列表。

超过 1000 条数据时，必须在 C++ 模型端实现：

- 分页加载；
- 增量加载；
- 懒加载；
- 局部刷新；
- 缓存管理。

delegate 必须保持轻量：

- 控件层级尽量浅；
- 避免复杂绑定；
- 避免复杂 JavaScript；
- 避免在 delegate 中加载重型组件；
- 图片必须异步加载并限制尺寸。

---

## 8. 图片规则

所有 QML `Image` 必须设置：

```qml
asynchronous: true
sourceSize.width: 显示宽度
sourceSize.height: 显示高度
```

禁止直接加载超大原图作为缩略图显示。

列表中的图片必须使用：

- 缩略图；
- 占位图；
- 异步加载；
- `sourceSize` 限制解码尺寸；
- 合理缓存。

超大图片、长图、地图、海报墙应使用分块加载、`QQuickImageProvider` 或 C++ 缓存服务。

---

## 9. 视频规则

视频播放必须使用：

```qml
MediaPlayer
VideoOutput
```

视频播放、预览、抽帧、缩略图生成、黑边检测、智能评分等重型逻辑必须由 C++ 后台线程处理。

视频不可见时必须暂停或释放资源。

禁止多个不可见视频继续播放或解码。

---

## 10. 动画规则

动画必须使用 Qt Quick 动画组件：

- `PropertyAnimation`
- `NumberAnimation`
- `Behavior`
- `Transition`
- `XAnimator`
- `YAnimator`
- `ScaleAnimator`
- `OpacityAnimator`

禁止使用 `Timer` 手动修改属性实现动画。

避免大量动画同时运行。

---

## 11. 懒加载规则

非首屏模块必须懒加载，例如：

- 设置页；
- 关于页；
- 弹窗；
- 高级面板；
- 视频详情页；
- 图片预览页；
- 重型图表；
- 插件模块。

推荐使用：

```qml
Loader {
    active: false
    asynchronous: true
}
```

应用启动阶段只加载首屏和核心服务，非关键初始化应延后或放入后台线程。

---

## 12. CMake 规则

项目必须使用 CMake。

新增 C++ 文件后必须同步更新对应 `CMakeLists.txt`。

第三方依赖应通过：

- `find_package`
- `FetchContent`
- 包管理器

进行管理，禁止随意复制到源码树。

Windows + Qt 构建时，应优先使用：

- Qt 自带 CMake；
- MSVC；
- Qt `msvc2022_64`；

禁止默认使用 MinGW。

---

## 13. AI 生成规则

AI 生成代码时必须自动判断文件归属：

- 程序入口、应用启动逻辑放入 `app/`；
- 核心业务逻辑放入 `src/core/`；
- Widgets、窗口、对话框、视图组件放入 `src/gui/`；
- 业务服务、下载、解析、任务调度放入 `src/service/`；
- 数据模型、状态模型、Qt Model/View 模型放入 `src/models/`；
- UI 与业务逻辑之间的协调层放入 `src/controller/`；
- 日志、配置、数据库、网络、文件系统放入 `src/infrastructure/`；
- Windows/macOS/Linux 平台差异代码放入 `src/platform/`；
- QML 页面放入 `qml/pages/`；
- QML 复用组件放入 `qml/components/`；
- QML 主题放入 `qml/themes/`；
- 图标、图片、字体、QRC 资源放入 `resources/`；
- 翻译文件放入 `translations/`；
- 项目文档放入 `docs/`；
- 脚本和开发工具放入 `tools/`；
- 插件放入 `plugins/`；
- 第三方依赖放入 `3rdparty/`，或通过包管理器管理。

AI 不得创建：

```text
aaa.cpp
test.qml
新建文件.qml
myclass.cpp
```

AI 生成界面时必须优先使用 QML；如果用户明确要求 Qt Widgets，则对应代码必须放入 `src/gui/`。

AI 生成业务、数据、算法、文件、网络、图片、视频处理时必须优先使用 C++。

AI 生成耗时逻辑时必须自动使用后台线程。

AI 生成新 C++ 文件时必须同步更新对应的 `CMakeLists.txt`，包括根目录、`app/` 或 `src/` 下的构建配置。

---

## 14. 代码审查规则

每次新增或修改代码后必须检查：

1. QML 是否包含业务逻辑；
2. QML 是否直接访问数据库、文件或网络；
3. C++ 是否直接操作 UI；
4. 大列表是否错误使用 `Repeater`；
5. `Image` 是否缺少 `asynchronous`；
6. `Image` 是否缺少 `sourceSize`；
7. delegate 是否过重；
8. 视频不可见时是否暂停；
9. 耗时任务是否阻塞 UI；
10. 后台线程是否直接操作 QML；
11. 新文件是否放在 `app/`、`src/core/`、`src/gui/`、`src/service/`、`src/models/`、`src/controller/`、`src/infrastructure/`、`src/platform/`、`qml/`、`resources/`、`translations/`、`docs/`、`tools/` 等正确目录；
12. 文件命名是否符合规范；
13. 头文件是否使用 `#pragma once`；
14. CMake 是否同步更新；
15. 资源是否放在 `resources/` 下。

---

## 15. 强制结论

本项目所有代码生成、修改、审查和重构必须遵守：

1. 界面使用 QML；
2. 逻辑使用 C++；
3. 数据使用 Qt Model；
4. 通信使用信号/槽和属性绑定；
5. 耗时任务使用后台线程；
6. 大列表使用虚拟视图；
7. 图片必须异步并限制解码尺寸；
8. 视频必须使用 Qt Multimedia；
9. 动画必须使用 Qt Quick 动画系统；
10. 非首屏模块必须懒加载；
11. 项目结构必须标准化；
12. 文件命名必须规范；
13. CMake 必须同步维护；
14. AI 生成代码必须自动放入当前规定的项目目录结构；
15. 每次修改后必须执行合规检查。
