---
description: 修改 CMake、添加模块、添加 C++ 文件、配置 Qt/MSVC 构建时使用
globs:
  - "**/CMakeLists.txt"
  - "cmake/**/*.cmake"
alwaysApply: false
---

# CMake 构建规则

## 1. 使用时机

当出现以下操作时使用本规则：

- 新增 C++ 文件；
- 新增模块；
- 修改 `CMakeLists.txt`；
- 添加 Qt 模块；
- 添加第三方依赖；
- 调整构建目录；
- 配置 Windows Qt 构建；
- 配置 app/src 子模块构建。

---

## 2. 基本规则

项目必须使用 CMake。

根目录 `CMakeLists.txt` 负责：

- 项目声明；
- Qt 模块查找；
- 添加子目录；
- 全局编译选项；
- 安装配置；
- 打包配置。

`app/` 和 `src/` 必须有独立 `CMakeLists.txt`。

---

## 3. 新文件规则

新增 C++ 文件后必须同步更新对应的 `CMakeLists.txt`。

示例：

- `app/main.cpp` 更新 `app/CMakeLists.txt`；
- `src/models/VideoListModel.cpp` 更新 `src/CMakeLists.txt`；
- 新模块需要时更新根目录 `CMakeLists.txt`。

禁止创建 C++ 文件后不加入构建系统。

---

## 4. Qt 构建规则

使用 Qt 6 时，必须明确查找需要的模块。

示例：

```cmake
find_package(Qt6 REQUIRED COMPONENTS Core Gui Qml Quick Multimedia)
```

根据实际代码使用模块，不要无意义引入大量 Qt 模块。

---

## 5. 第三方依赖规则

第三方依赖优先使用：

```cmake
find_package()
FetchContent
包管理器
```

不建议直接把大型第三方源码复制进项目。

如必须放入源码树，应放入：

```text
3rdparty/
```

并明确版本、许可证和编译开关。

---

## 6. Windows Qt 构建规则

Windows + Qt 构建时，应优先使用：

- Qt 自带 CMake；
- MSVC；
- Qt `msvc2022_64`。

禁止默认使用 MinGW。

日常构建目录与打包构建目录应分离。

---

## 7. CMake 审查清单

修改 CMake 后必须检查：

1. 新增 C++ 文件是否加入构建；
2. Qt 模块是否完整且不过量；
3. `app/` 和 `src/` 是否职责清晰；
4. 第三方依赖是否通过规范方式管理；
5. Windows 是否使用 MSVC + Qt `msvc2022_64`；
6. 构建目录是否没有污染源码目录。
