---
description: 执行任务时使用；规定执行计划、执行文档、状态更新、验证流程和 Qt6 构建环境
globs:
  - "**/*.cpp"
  - "**/*.h"
  - "**/*.hpp"
  - "**/*.qml"
  - "**/*.cmake"
  - "**/CMakeLists.txt"
  - "**/*.md"
  - "**/*.ps1"
  - "**/*.cmd"
alwaysApply: false
---

# 执行规则

## 1. 使用时机

当执行以下任务时，必须使用本规则：

- 创建、修改、删除、移动文件；
- 修改 C++ / QML / CMake / 脚本 / Markdown；
- 编译项目；
- 修复编译或运行问题；
- 制定执行计划。

---

## 2. 核心要求

- 每次制定执行计划后，必须创建或更新执行文档。
- 执行文档必须先于代码修改、文件创建和编译。
- 必须按执行文档中的步骤顺序执行，禁止跳步。
- 每个步骤完成后必须验证，验证通过后才能继续下一步。
- 无法自动验证时，必须让用户确认后才能标记完成。
- 执行过程中必须实时更新步骤状态，禁止最后一次性补状态。
- 编译失败时，必须把错误和修复步骤追加到执行文档。

---

## 3. 执行文档位置

执行文档统一保存到：

```text
docs/execution/
```

命名格式：

```text
YYYYMMDD_任务简述.md
```

示例：

```text
docs/plan/20260703_fix_build_error.md
docs/plan/20260703_add_share_service.md
```

要求：

- 文件名使用小写字母；
- 多个单词使用下划线；
- 使用 `.md` 后缀；
- 使用 UTF-8 编码。

---

## 4. 执行文档格式

每个执行文档必须包含以下结构：

```markdown
# 执行文档：任务名称

## 0. 问题概述

### 主要问题

1. xxx
2. xxx

### 解决思路

xxx

## 1. 执行步骤

### 步骤 1：xxx

- 修改内容：具体文件和具体修改内容
- 难易程度：低 / 中 / 高
- 完成状态：未开始 / 进行中 / 阻塞 / 完成
- 验证方式：xxx

### 步骤 2：xxx

- 修改内容：具体文件和具体修改内容
- 难易程度：低 / 中 / 高
- 完成状态：未开始 / 进行中 / 阻塞 / 完成
- 验证方式：xxx
```

---

## 5. 状态规则

步骤状态只能使用：

- 未开始
- 进行中
- 阻塞
- 完成

执行要求：

- 开始步骤前，状态改为“进行中”；
- 验证通过后，状态改为“完成”；
- 验证失败时，保持“进行中”或改为“阻塞”；
- 需要用户确认时，状态必须为“阻塞”。

---

## 6. 验证规则

每一步都必须有验证方式。

常用验证方式：

- 检查文件存在；
- 检查代码格式；
- CMake 配置通过；
- 编译通过；
- 程序启动正常；
- 功能行为符合预期；
- 用户确认。

验证失败时：

1. 记录失败原因；
2. 追加修复步骤；
3. 修复后重新验证；
4. 验证通过后继续下一步。

---

## 7. 项目环境

- 操作系统：Windows 10+
- Qt 版本：6.8.3
- 编译器：MSVC 2022
- 构建系统：CMake 3.30
- 构建器：Ninja

---

## 8. 工具路径

| 工具 | 路径 |
|---|---|
| Qt 6.8.3 | `C:\Qt\6.8.3\msvc2022_64\bin` |
| Qt Creator | `C:\Qt\Tools\QtCreator\bin` |
| CMake 3.30 | `C:\Qt\Tools\CMake_64\bin` |
| Ninja | `C:\Qt\Tools\Ninja` |
| MSVC 2022 | `C:\Program Files\Microsoft Visual Studio\2022\Community\VC\Auxiliary\Build` |

禁止假设工具已加入系统 PATH，执行命令时优先使用完整路径。

---

## 9. 编译命令

修改 `CMakeLists.txt` 或 `.cmake` 后，先配置：

```powershell
& "C:\Qt\Tools\CMake_64\bin\cmake.exe" -B "d:\qt6cmake\XX\build\Desktop_Qt_6_8_3_MSVC2022_64bit-Release" -S "d:\qt6cmake\XX"
```

编译项目：

```powershell
& "C:\Qt\Tools\CMake_64\bin\cmake.exe" --build "d:\qt6cmake\XX\build\Desktop_Qt_6_8_3_MSVC2022_64bit-Release" --config Release
```

规则：

- 修改 CMake 后必须先配置再编译；
- 只修改源码时可以直接编译；
- 编译失败不得标记完成；
- 修复后必须重新编译验证。

---

## 10. 编码要求

所有项目文件必须使用 UTF-8 编码，包括：

- C++ 文件；
- QML 文件；
- CMake 文件；
- Markdown 文件；
- PowerShell / CMD 脚本。

禁止因编码问题导致中文注释、中文字符串或脚本输出乱码。

---

## 11. 冲突处理

- 冲突和遗漏必须直接加入执行步骤；
- 有唯一合理方案时直接采用；
- 没有明显最优方案时，给出推荐方案并让用户决定；
- 用户未确认前，相关步骤状态为“阻塞”。

---

## 12. 禁止行为

禁止：

- 有执行计划但不创建执行文档；
- 未创建执行文档就修改代码；
- 跳过验证；
- 验证失败仍继续下一步；
- 编译失败却标记完成；
- 最后一次性补状态；
- 修改 CMake 后不重新配置；
- 使用模糊描述代替具体修改内容；
- 未经用户确认就完成无法自动验证的步骤。
