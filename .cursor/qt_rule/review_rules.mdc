---
description: 代码审查、重构检查、合规检查、提交前检查时使用
globs:
  - "**/*.cpp"
  - "**/*.h"
  - "**/*.hpp"
  - "**/*.qml"
  - "**/CMakeLists.txt"
alwaysApply: false
---

# 审查与合规规则

## 1. 使用时机

当进行以下操作时使用本规则：

- 代码审查；
- 重构；
- 提交前检查；
- 架构合规检查；
- 性能检查；
- AI 修改代码后的自检；
- 修复规则违规。

---

## 2. 架构检查

必须检查：

1. QML 是否只负责界面；
2. C++ 是否负责业务逻辑；
3. 数据是否通过 Qt Model 暴露；
4. QML 与 C++ 是否通过信号/槽、属性绑定、模型通知通信；
5. 耗时任务是否在后台线程；
6. C++ 是否直接操作 QML UI；
7. QML 是否直接访问数据库、文件或网络。

---

## 3. 目录与命名检查

必须检查：

1. 新文件是否放在正确目录；
2. C++ 文件名是否与主类名一致；
3. QML 文件是否使用大驼峰；
4. 目录是否使用小写蛇形；
5. 测试文件是否使用 `test_<被测对象名>.cpp`；
6. 是否出现无意义命名；
7. 是否有源码文件散落在根目录。

禁止出现：

```text
aaa.cpp
test.qml
新建文件.qml
myclass.cpp
temp.cpp
```

---

## 4. QML 检查

必须检查：

1. QML 是否包含业务逻辑；
2. QML 是否直接访问数据库、文件或网络；
3. QML 是否有复杂 JavaScript；
4. 大列表是否错误使用 `Repeater`；
5. `Image` 是否缺少 `asynchronous`；
6. `Image` 是否缺少 `sourceSize`；
7. delegate 是否过重；
8. 视频不可见时是否暂停；
9. 非首屏模块是否懒加载；
10. 是否有不必要的 `clip: true`、模糊、阴影、透明叠加。

---

## 5. C++ 检查

必须检查：

1. C++ 是否直接操作 UI；
2. C++ 是否把界面样式写入后端；
3. 是否有耗时槽函数阻塞 UI；
4. 后台线程是否直接操作 UI；
5. 是否通过信号回传结果；
6. 模型通知是否精准；
7. 是否有资源泄漏风险；
8. 头文件是否使用 `#pragma once`；
9. 公共 API 是否有必要注释。

---

## 6. 性能检查

必须检查：

1. 超过 20 条数据是否使用 `ListView` / `GridView` / `TableView`；
2. 超过 1000 条数据是否分页或增量加载；
3. 图片是否异步加载并限制解码尺寸；
4. 视频是否不可见时暂停；
5. 动画是否使用 Qt Quick 动画系统；
6. 非首屏模块是否懒加载；
7. 高频更新是否节流或合并；
8. C++ 是否管理缓存和资源释放。

---

## 7. CMake 检查

必须检查：

1. 新增 C++ 文件是否更新 `CMakeLists.txt`；
2. Qt 模块是否正确；
3. 第三方依赖是否规范；
4. `app/` 和 `src/` 构建边界是否清晰；
5. Windows 构建是否优先使用 MSVC + Qt `msvc2022_64`。

---

## 8. 强制结论

每次修改后必须确认：

1. 界面使用 QML；
2. 逻辑使用 C++；
3. 数据使用 Qt Model；
4. 通信使用信号/槽和属性绑定；
5. 耗时任务使用后台线程；
6. 大列表使用虚拟视图；
7. 图片异步并限制解码尺寸；
8. 视频使用 Qt Multimedia；
9. 动画使用 Qt Quick 动画系统；
10. 非首屏模块懒加载；
11. 项目结构符合规范；
12. 文件命名符合规范；
13. CMake 已同步维护。
