---
description: 编写或修改 C++ 业务逻辑、服务、模型、控制器、线程任务时使用
globs:
  - "app/**/*.cpp"
  - "app/**/*.h"
  - "app/**/*.hpp"
  - "src/**/*.cpp"
  - "src/**/*.h"
  - "src/**/*.hpp"
alwaysApply: false
---

# C++ 逻辑规则

## 1. 使用时机

当编写或修改以下内容时使用本规则：

- C++ 业务逻辑；
- C++ 服务；
- C++ Controller；
- Qt Model；
- 数据库访问；
- 网络请求；
- 文件读写；
- 图片处理；
- 视频处理；
- OpenCV / FFmpeg 处理；
- 后台线程任务；
- 系统或平台代码。

---

## 2. C++ 职责

以下内容必须放入 C++：

- 业务逻辑；
- 数据处理；
- 模型管理；
- 网络请求；
- 数据库访问；
- 文件系统操作；
- 图片处理；
- 视频处理；
- 算法计算；
- 缓存管理；
- 系统交互；
- 硬件调用；
- 资源管理；
- 耗时任务。

---

## 3. C++ 与 QML 交互方式

C++ 向 QML 暴露功能时，应使用：

```cpp
Q_PROPERTY
Q_INVOKABLE
public slots
signals
QAbstractListModel
QAbstractTableModel
qmlRegisterType
setContextProperty
```

属性用于状态绑定。

槽函数或 `Q_INVOKABLE` 用于触发动作。

信号用于通知 QML 状态变化。

模型用于向 QML 提供列表和表格数据。

---

## 4. 模型规则

列表和表格数据必须优先使用 Qt Model/View 模型。

推荐：

```cpp
QAbstractListModel
QAbstractTableModel
```

禁止直接把大量 `QVariantList`、JSON 数组或字符串数组塞给 QML 后再做复杂处理。

超过 1000 条数据时，模型必须支持：

- 分页加载；
- 增量加载；
- 懒加载；
- 局部刷新；
- 缓存管理；
- 可取消请求；
- 精准 `dataChanged` 通知。

---

## 5. 线程规则

任何可能阻塞 UI 的任务必须放到后台线程，例如：

- 数据库查询；
- 文件扫描；
- 网络请求；
- 图片处理；
- 视频处理；
- 大数据计算；
- OpenCV / FFmpeg 处理。

推荐使用：

```cpp
QThread
QThreadPool
QtConcurrent
QObject::moveToThread
```

后台任务完成后必须通过信号通知 QML。

禁止后台线程直接操作 QML 或 UI 对象。

---

## 6. C++ 禁止事项

禁止 C++ 直接承担普通界面职责，例如：

- 直接设置 QML 控件颜色；
- 直接控制 QML 控件布局；
- 在业务类中创建具体页面；
- 在业务类中操作 `QQuickItem`；
- 把界面样式写入 C++。

除非确实需要动态加载 QML 组件，否则不得在 C++ 中直接构建 QML UI。

---

## 7. Controller 规则

`src/controller/` 用于 UI 与业务逻辑之间的协调。

Controller 可以：

- 接收 UI 请求；
- 调用 service；
- 更新 model；
- 暴露状态给 QML；
- 转换 UI 需要的数据格式。

Controller 不应：

- 直接写数据库底层实现；
- 直接写网络底层实现；
- 直接处理大型算法；
- 直接操作 QML 控件样式。

---

## 8. Service 规则

`src/service/` 用于业务服务。

Service 可以包含：

- 下载；
- 解析；
- 任务调度；
- 缩略图生成；
- 视频分析；
- 业务流程处理。

Service 不应依赖具体 QML 页面。

---

## 9. Infrastructure 规则

`src/infrastructure/` 用于基础设施。

包括：

- 日志；
- 配置；
- 数据库；
- 网络；
- 文件系统；
- 缓存；
- 本地存储。

业务层应通过接口或服务封装使用基础设施能力，避免到处散落数据库、网络、文件访问代码。

---

## 10. C++ 审查清单

修改 C++ 后必须检查：

1. 是否把业务逻辑放在 C++；
2. 是否直接操作 QML UI；
3. 是否有耗时任务阻塞 UI；
4. 后台线程是否直接操作 UI；
5. 是否通过信号回传结果；
6. 模型通知是否精准；
7. 新文件是否同步更新 CMake；
8. 公共头文件是否使用 `#pragma once`；
9. 公共 API 是否有必要注释；
10. 资源是否及时释放。
