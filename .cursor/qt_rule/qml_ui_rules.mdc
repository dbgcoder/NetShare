---
description: 编写或修改 QML 页面、组件、主题、动画、布局时使用
globs:
  - "qml/**/*.qml"
alwaysApply: false
---

# QML UI 规则

## 1. 使用时机

当编写或修改以下内容时使用本规则：

- QML 页面；
- QML 组件；
- QML 主题；
- QML delegate；
- QML 动画；
- QML 布局；
- QML 交互；
- QML 图片展示；
- QML 视频播放界面。

---

## 2. QML 职责

QML 只负责：

- 窗口和页面展示；
- 控件组合；
- 布局；
- 动画；
- 手势；
- 状态展示；
- 简单表单校验；
- 简单页面切换；
- 主题和视觉样式；
- delegate 展示。

---

## 3. QML 禁止事项

QML 中禁止直接包含：

- 业务规则；
- 数据库操作；
- 文件读写；
- 网络请求；
- 图像处理；
- 视频处理；
- OpenCV / FFmpeg 调用；
- 复杂计算；
- 大型循环；
- 递归算法；
- 复杂排序；
- 复杂过滤；
- 持久化逻辑。

如果 QML 中出现超过 5 行且涉及数据处理的 JavaScript 函数，应迁移到 C++。

---

## 4. 布局规则

优先使用声明式布局：

```qml
RowLayout
ColumnLayout
GridLayout
anchors
Layout.fillWidth
Layout.fillHeight
```

避免大量 JavaScript 手动计算：

```qml
x
y
width
height
```

---

## 5. 主题规则

颜色、字体、圆角、间距、图标尺寸等视觉参数应统一放入主题或组件属性。

推荐：

```text
qml/themes/AppTheme.qml
```

禁止在大量页面中散落硬编码颜色、字体和尺寸。

---

## 6. 图片规则

所有 QML `Image` 必须设置：

```qml
asynchronous: true
sourceSize.width: 显示宽度
sourceSize.height: 显示高度
```

禁止直接加载超大原图作为缩略图。

列表中的图片必须使用：

- 缩略图；
- 占位图；
- 异步加载；
- `sourceSize` 限制解码尺寸；
- 合理缓存。

---

## 7. 视频规则

视频播放界面必须使用：

```qml
MediaPlayer
VideoOutput
```

视频不可见时必须暂停或释放资源。

禁止多个不可见视频继续播放或解码。

视频抽帧、缩略图生成、黑边检测、智能评分等重型逻辑必须交给 C++ 后台线程。

---

## 8. 动画规则

动画必须使用 Qt Quick 动画组件：

```qml
PropertyAnimation
NumberAnimation
Behavior
Transition
XAnimator
YAnimator
ScaleAnimator
OpacityAnimator
```

禁止使用 `Timer` 手动修改属性实现动画。

避免大量动画同时运行。

---

## 9. 懒加载规则

非首屏模块必须懒加载，例如：

- 设置页；
- 关于页；
- 弹窗；
- 高级面板；
- 视频详情页；
- 图片预览页；
- 重型图表；
- 插件模块。

推荐：

```qml
Loader {
    active: false
    asynchronous: true
}
```

应用启动阶段只加载首屏和核心服务，非关键初始化应延后或放入后台线程。

---

## 10. QML 审查清单

修改 QML 后必须检查：

1. 是否包含业务逻辑；
2. 是否直接访问数据库、文件或网络；
3. 是否有复杂 JavaScript；
4. 是否错误使用 `Repeater` 渲染大列表；
5. `Image` 是否设置 `asynchronous`；
6. `Image` 是否设置 `sourceSize`；
7. delegate 是否过重；
8. 视频不可见时是否暂停；
9. 非首屏重型模块是否懒加载；
10. 是否有不必要的 `clip: true` 或多层透明叠加。
