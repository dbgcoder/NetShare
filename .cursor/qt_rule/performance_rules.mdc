---
description: 处理大列表、图片、视频、动画、懒加载、启动性能和资源缓存时使用
globs:
  - "qml/**/*.qml"
  - "src/**/*.cpp"
  - "src/**/*.h"
  - "src/**/*.hpp"
alwaysApply: false
---

# 性能规则

## 1. 使用时机

当实现或优化以下内容时使用本规则：

- 大数据列表；
- 表格；
- 图片加载；
- 视频播放；
- 视频抽帧；
- 缩略图；
- 动画；
- 懒加载；
- 启动优化；
- 缓存；
- 后台任务；
- OpenCV / FFmpeg 处理。

---

## 2. 大数据列表规则

超过 20 条数据必须使用：

```qml
ListView
GridView
TableView
```

禁止使用：

```qml
Repeater
Column + Repeater
Row + Repeater
Grid + Repeater
```

渲染大列表。

超过 1000 条数据时，必须在 C++ 模型端实现：

- 分页加载；
- 增量加载；
- 懒加载；
- 局部刷新；
- 缓存管理。

---

## 3. Delegate 性能规则

delegate 必须保持轻量：

- 控件层级尽量浅；
- 避免复杂嵌套；
- 避免复杂绑定；
- 避免复杂 JavaScript；
- 避免每个 delegate 都使用 `Loader`；
- 避免大量阴影、模糊、透明叠加；
- 图片必须异步加载并限制尺寸。

---

## 4. 图片性能规则

所有 QML `Image` 必须设置：

```qml
asynchronous: true
sourceSize.width: 显示宽度
sourceSize.height: 显示高度
```

禁止把超大原图直接当缩略图显示。

列表图片必须使用：

- 缩略图；
- 占位图；
- 异步加载；
- 解码尺寸限制；
- 合理缓存。

超大图片、长图、地图、海报墙应使用：

- 分块加载；
- `QQuickImageProvider`；
- C++ 缓存服务；
- LRU 缓存；
- 可见区域加载。

---

## 5. 视频性能规则

视频播放必须使用：

```qml
MediaPlayer
VideoOutput
```

视频相关重型逻辑必须由 C++ 后台线程处理：

- 抽帧；
- 缩略图生成；
- 黑边检测；
- 智能评分；
- 元数据解析；
- 转码；
- OpenCV / FFmpeg 处理。

视频不可见时必须暂停或释放资源。

列表页、网格页不得同时播放多个视频。默认显示缩略图，选中或悬停时再预览。

---

## 6. 动画性能规则

动画必须使用 Qt Quick 动画系统：

```qml
PropertyAnimation
NumberAnimation
Behavior
Transition
XAnimator
YAnimator
ScaleAnimator
OpacityAnimator
```

禁止使用 `Timer` 高频修改属性实现动画。

简单位移、缩放、透明度动画优先使用 `Animator` 系列。

避免大量动画同时运行。

---

## 7. 懒加载规则

非首屏模块必须懒加载：

```qml
Loader {
    active: false
    asynchronous: true
}
```

适用：

- 设置页；
- 关于页；
- 弹窗；
- 高级筛选面板；
- 视频详情页；
- 图片预览页；
- 插件页；
- 报表页；
- 大型图表页。

---

## 8. 启动优化规则

启动阶段只加载必要内容：

- 主窗口；
- 首屏页面；
- 核心后端；
- 必要配置；
- 基础主题；
- 必要模型。

非关键任务应延迟或异步：

- 扫描大型目录；
- 生成缩略图；
- 检查更新；
- 加载历史记录；
- 统计分析；
- 云同步；
- 预加载后续页面。

---

## 9. 通用渲染规则

应减少：

- 不必要的 `clip: true`；
- 多层透明叠加；
- 大面积模糊；
- 大量阴影；
- 多层嵌套；
- 复杂遮罩；
- 大量不可见对象仍保持 active。

高频属性变化应合并或节流，可使用：

```qml
Qt.callLater(updateFunction)
```

---

## 10. 资源管理规则

C++ 后端应提供资源管理能力，管理：

- 图片缓存；
- 缩略图缓存；
- 视频句柄；
- 数据模型缓存；
- 临时文件；
- 后台任务；
- 网络请求；
- 大对象生命周期。

推荐使用 LRU、引用计数或闲置资源释放机制。
