---
description: Qt/QML 项目总规则；始终加载，并按任务场景引用其他 MDC 规则
globs:
  - "**/*"
alwaysApply: true
---

# Qt/QML 项目总规则

## 1. 总原则

本项目采用 Qt/QML + C++ 商用软件架构。

必须长期遵循：

```text
界面 QML 优先
逻辑 C++ 优先
数据 Model 化
通信信号/槽化
耗时任务线程化
资源管理集中化
目录命名规范化
CMake 构建标准化
```

QML 负责界面展示、交互、布局、动画和视觉样式。

C++ 负责业务逻辑、数据处理、模型管理、数据库、网络、文件系统、图片处理、视频处理、算法、缓存和耗时任务。

禁止把业务逻辑写进 QML。

禁止把界面样式和控件操作写进 C++ 后端。

---

## 2. 规则调用方式

本文件始终加载，只负责总原则和规则路由。

执行具体任务时，必须根据任务类型按需参考对应 MDC 文件。

---

## 3. 创建类、文件、函数、组件时

当任务涉及以下内容时，必须调用：

```text
qt_naming_rule.mdc
```

适用场景：

- 创建新文件；
- 创建新目录；
- 创建 C++ 类；
- 创建 C++ 函数；
- 创建 C++ 成员变量；
- 创建 QML 页面；
- 创建 QML 组件；
- 创建 QML 属性；
- 创建 QML 信号；
- 创建测试文件；
- 移动文件；
- 重命名文件；
- 判断文件应该放在哪里；
- 判断类、函数、变量、QML 组件应该如何命名。

调用目的：

- 确认文件放置目录；
- 确认命名是否符合规范；
- 确认是否需要同步更新 CMake；
- 避免在根目录乱放文件；
- 避免创建无意义命名。

---

## 4. 编写或修改 QML 时

当任务涉及以下内容时，必须调用：

```text
qml_ui_rules.mdc
```

适用场景：

- QML 页面；
- QML 组件；
- QML 布局；
- QML 动画；
- QML 图片展示；
- QML 视频播放界面；
- QML delegate；
- QML 主题；
- QML 交互；
- QML 懒加载。

调用目的：

- 保证 QML 只做 UI；
- 避免 QML 写业务逻辑；
- 避免 QML 直接访问数据库、文件、网络；
- 保证图片、视频、动画和布局符合性能规则。

---

## 5. 编写或修改 C++ 逻辑时

当任务涉及以下内容时，必须调用：

```text
cpp_logic_rules.mdc
```

适用场景：

- C++ 业务逻辑；
- C++ 服务；
- C++ Controller；
- Qt Model；
- 数据库访问；
- 网络请求；
- 文件读写；
- 图片处理；
- 视频处理；
- OpenCV / FFmpeg 处理；
- 后台线程任务；
- 系统或平台代码。

调用目的：

- 保证业务逻辑放在 C++；
- 保证 C++ 与 QML 通过属性、信号、槽、模型通信；
- 保证耗时任务放入后台线程；
- 避免 C++ 后端直接操作 QML UI。

---

## 6. 处理性能问题时

当任务涉及以下内容时，必须调用：

```text
performance_rules.mdc
```

适用场景：

- 大列表；
- 表格；
- 图片加载；
- 视频播放；
- 视频抽帧；
- 缩略图生成；
- 动画；
- 懒加载；
- 启动优化；
- 缓存；
- 后台任务；
- OpenCV / FFmpeg 处理；
- UI 卡顿；
- 内存占用过高。

调用目的：

- 保证大数据使用虚拟视图和分页；
- 保证图片异步加载并限制解码尺寸；
- 保证视频不可见时暂停或释放；
- 保证动画不阻塞 UI；
- 保证非首屏模块懒加载；
- 保证资源可释放、可缓存、可控制。

---

## 7. 修改 CMake 或新增 C++ 文件时

当任务涉及以下内容时，必须调用：

```text
cmake_rules.mdc
```

适用场景：

- 新增 C++ 文件；
- 新增模块；
- 修改 `CMakeLists.txt`；
- 添加 Qt 模块；
- 添加第三方依赖；
- 调整构建目录；
- 配置 Windows Qt 构建；
- 配置 app/src 子模块构建。

调用目的：

- 保证新 C++ 文件加入构建；
- 保证 app/src 模块结构清晰；
- 保证 Qt 模块声明正确；
- 保证 Windows 使用 MSVC + Qt `msvc2022_64`；
- 避免 CMake 与源码不同步。

---

## 8. 做代码审查、重构、自检时

当任务涉及以下内容时，必须调用：

```text
review_rules.mdc
```

适用场景：

- 代码审查；
- 重构；
- 提交前检查；
- 架构合规检查；
- 性能检查；
- AI 修改代码后的自检；
- 修复规则违规。

调用目的：

- 检查 QML/C++ 分层是否正确；
- 检查目录和命名是否合规；
- 检查性能规则是否违反；
- 检查 CMake 是否同步；
- 检查是否存在阻塞 UI、资源泄漏、错误线程访问等问题。

---

## 9. 多规则组合调用

同一个任务可以同时调用多个规则。

示例：

### 创建一个视频列表页面

必须同时参考：

```text
qt_naming_rule.mdc
qml_ui_rules.mdc
cpp_logic_rules.mdc
performance_rules.mdc
cmake_rules.mdc
```

原因：

- 需要创建文件，必须判断目录和命名；
- 需要 QML 页面，必须遵守 QML UI 规则；
- 需要 C++ Model 或 Controller，必须遵守 C++ 逻辑规则；
- 视频列表涉及图片、缩略图和大列表，必须遵守性能规则；
- 新增 C++ 文件，必须更新 CMake。

### 修改一个 QML 按钮样式

通常参考：

```text
qml_ui_rules.mdc
```

如果创建了新按钮组件，还必须参考：

```text
qt_naming_rule.mdc
```

### 新增下载服务

必须同时参考：

```text
qt_naming_rule.mdc
cpp_logic_rules.mdc
cmake_rules.mdc
```

如果涉及性能、线程或大文件下载，还必须参考：

```text
performance_rules.mdc
```

### 做提交前检查

必须参考：

```text
review_rules.mdc
```

---

## 10. 强制执行顺序

处理任务时必须按以下顺序思考：

1. 这次任务属于什么类型？
2. 应该调用哪些 MDC 规则？
3. 是否需要创建新文件？
4. 新文件应该放在哪个目录？
5. 命名是否符合规范？
6. 是否涉及 QML？
7. 是否涉及 C++ 业务逻辑？
8. 是否涉及大数据、图片、视频、动画或耗时任务？
9. 是否需要更新 CMake？
10. 修改完成后是否需要审查规则自检？

---

## 11. 最低强制要求

无论是否显式调用其他规则，以下要求始终生效：

1. 界面使用 QML；
2. 逻辑使用 C++；
3. 数据使用 Qt Model；
4. 通信使用信号/槽和属性绑定；
5. 耗时任务使用后台线程；
6. 大列表使用虚拟视图；
7. 图片异步加载并限制解码尺寸；
8. 视频使用 Qt Multimedia；
9. 动画使用 Qt Quick 动画系统；
10. 非首屏模块懒加载；
11. 项目结构符合 `qt_directory_rule.mdc`；
12. 文件命名符合 `qt_naming_rule.mdc`；
13. CMake 变更符合 `cmake_rules.mdc`；
14. 修改后按需使用 `review_rules.mdc` 自检。
