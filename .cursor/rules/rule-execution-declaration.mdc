---
description: 执行前声明、执行披露、重点问句直接结论；根因优先与最小改动；索引全仓规则；不符合须说明原因。
alwaysApply: true
---

# 规则执行声明（全工程永久有效）

AI 每次执行任务时**必须**遵守本规则，不得例外。

## 0. 规则分类索引（合并后）

| 分类 | 规则文件 | 说明 |
|------|----------|------|
| **AI 行为与回复** | `rule-execution-declaration.mdc` | 本文 §1～§8：声明、索引表、不符合处理、示例、**执行披露**、**直接结论**、**根因分析·最小改动·禁止擅自兜底**、**删除=彻底移除（§8）** |
| **数据、AI 与时间** | `ai-data-integrity.mdc` | 原文/模型结果/回填/持久化 + **§7 时间统一**（原 `time-unification.mdc` 已并入） |
| **崩溃安全工作流** | `crash-safe-workflow.mdc` | 步骤拆分、`docs/*-analysis.md` 断点、双保存 |
| **架构** | `architecture.mdc` | QML 界面 / C++ 逻辑、性能 |
| **调试与日志** | `debug-logging.mdc` | Qt Creator Application Output、tag、脱敏 |
| **本机构建** | `cmake-qt-tools.mdc` | Windows CMake + MSVC + Qt 路径 |
| **云函数打包与上传** | `cloudfunc-auto-package.mdc` | Go 编译、zip、`buildTag`、须本人上传 |
| **超管审计可见性** | `super-user-audit-visibility.mdc` | 超级用户须可看**全用户**积分流水与任务记录；禁止擅自收窄为仅本人 |
| **界面改动同意** | `ui-changes-consent.mdc` | 未经用户明确同意，禁止在 QML 界面擅自新增标题、按钮等可见控件；需加则先征得同意 |
| **根因优先·禁糊弄展示** | `root-cause-before-display-fix.mdc` | 所有提问按读→源→写查因；**禁止**只改显示结果掩盖数据/写库问题 |
| **取证后再改** | `verify-before-change.mdc` | 每步先验证是根因再改；无验证不改；改后再验 |
| **结论围绕问题·取证再验** | `rule-execution-declaration.mdc` **§6.6** | 「直接结论」须围绕用户提问，写明验证了什么、证明问题在哪、改后再验结果；禁止以利迭为主轴 |

---

## 1. 执行前声明

每次开始执行（修改代码、分析问题、写文档等）时，**必须先声明**本次使用的规则，格式示例：

```
【本次使用规则】
- crash-safe-workflow：从 docs/xxx-analysis.md 断点继续，每步完成后更新文档
- architecture：界面 QML / 逻辑 C++，检查点 A/B/C...
- ai-data-integrity：原文不改、模型结果原样保存、回填检查点；时间字段见同文件 §7
- debug-logging：Qt Creator 控制台、格式与脱敏，见 debug-logging.mdc
- rule-execution-declaration：§5 执行披露、§6 重点问句须「## 直接结论」（若触发）、**§6.6 结论围绕用户问题并写明验证/根因/再验**、§7 先查根因/最小改动/勿擅自做兜底回退、§8 用户说「删除」时彻底移除不做兼容
```

- 未使用的规则可不列，但**使用了就必须写出来**。
- 方便用户核对与提醒，防止 AI 遗忘或乱改。

---

## 2. 核心规则（严格执行）

| 规则 | 触发场景 | 必须动作 |
|------|----------|----------|
| **crash-safe-workflow** | 用户说「继续」、异常恢复、多步骤任务 | 读 docs/*-analysis.md → 提取断点 → 从断点继续；每步完成立即勾选、更新断点、落盘文档 |
| **architecture** | 新建/修改 C++ 或 QML、涉及界面或逻辑 | 界面用 QML、逻辑用 C++；检查点 A~I，不符合则修正或说明原因 |
| **ai-data-integrity** | AI 请求/响应、回填、持久化、分镜/人物/物品/场景增删改 | 原文不改、模型结果原样保存；逐条对照检查点，不符合则修正或说明原因 |
| **debug-logging** | 加控制台调试、链路日志、排序/密钥排查 | 见 `.cursor/rules/debug-logging.mdc`：仅 Application Output、统一 `[ISO][模块][tag]`、密钥脱敏 |
| **time-unification**（并入 **ai-data-integrity §7**） | 涉及时间字段（创建/修改/删除/写库/显示/排序/筛选） | 见 `.cursor/rules/ai-data-integrity.mdc` **§7**：统一本地 `yyyy-MM-dd HH:mm:ss`；写库/显示/排序前标准化；默认执行 |
| **focused-answer-summary**（本文 **§6**） | 用户重点追问结论（可不可以/是不是/有没有等） | 文末 **`## 直接结论`**；第一句为明确是/否或可/不可；禁止把唯一答案只藏在长篇分析里 |
| **root-cause-minimal-scope**（本文 **§7**） | 排错、改 bug、用户质疑「为何显示/算错」 | **先分析根因**（数据源、运算/业务链路），**禁止**只改显示层糊弄正确结果；**最小改动**；用户**未要求**兜底/回退时 **禁止**实现 |
| **hard-delete-no-shim**（本文 **§8**） | 用户明确要求 **删除 / 去掉 / 移除** 某能力、字段、接口、文档表述等（含「像没加过一样」「不要兼容」） | **全仓清理**所有相关点；**禁止**兼容层、双路径、别名回落、deprecated 分支、读写旧键、单独迁移脚本（除非用户**另行**明确要求）；契约与 `docs/` 同步删净 |

---

## 3. 不符合规则时的处理

当无法完全符合某条规则时，**必须明确说明**：

- 违反了哪条规则
- 具体原因（技术限制、历史遗留、用户明确要求等）
- 是否有替代方案或后续补救

**禁止**： silently 违反规则、不说明原因直接修改。

---

## 4. 示例

**正确**：
> 【本次使用规则】crash-safe-workflow、ai-data-integrity  
> 从 storyboard-lines-regen-analysis.md 断点继续，步骤 20 为人物描述/图片提示词回填。修改 prompt 与解析逻辑，符合 ai-data-integrity 检查点。

**错误**：
> （无声明）直接改 ProjectPage.qml 的 _parseRegeneratedLines...

---

## 5. AI 执行披露（全工程永久有效）

*本节由原 `agent-execution-disclosure.mdc` 全文并入。*

AI 在每次执行代码修改或任务推进时，**必须**在回复开头或执行说明中明确写出：

### 5.1 本次使用的规则

用列表形式声明，例如：

- **规则来源**：`crash-safe-workflow.mdc` / `architecture.mdc` / `ai-data-integrity.mdc`
- **具体条目**：如「异常退出后从断点恢复」「界面 QML 优先、逻辑 C++ 优先」「回填只按模型结果写入」

### 5.2 执行动作与规则对应

说明本次操作对应规则的哪一条，例如：

- 收到「继续」→ 先读 `docs/*-analysis.md`，提取断点，从断点继续
- 涉及架构 → 检查 QML/C++ 职责划分
- 涉及 AI 请求/回填/持久化 → 按 `ai-data-integrity.mdc` 检查点核对
- 用户报告异常/显示不对 → 按本文 **§7** 先查根因，再改最小必要处

### 5.3 不符合规则时的说明

若某处无法按规则执行，**必须明确写出**：

- **不符合的规则**：哪条规则
- **原因**：为何无法遵守（如历史代码、第三方限制、用户明确要求等）
- **替代方案**：采取了什么替代做法

### 5.4 目的

- 防止 AI 遗忘规则、 silently 乱改
- 方便用户看到规则使用情况，及时提醒或纠正
- 不符合时说明原因，便于追溯和决策

---

## 6. 重点提问 · 明确结论（全工程永久有效）

*本节由原 `focused-answer-summary.mdc` 全文并入。*

### 6.1 触发条件

当用户问题属于下列之一（或语义等价）时，必须遵守本节：

- 要求 **结论 / 可不可以 / 是不是 / 行不行 / 对不对 / 有没有**
- 用 **「重点」「直接说」「别绕」「一句话」** 等强调只要答案
- 短句判断型问题，且 **不需要** 先看完整分析才能作答的（若必须先分析，仍须在分析后给本节规定的结论块）

### 6.2 必须怎么做（总结区）

在回复的 **总结部分**（推荐放在 **全文最后**，或紧接在极简背景之后）增加固定小节，使用 **二级标题**，标题中须含 **「直接结论」** 四字，例如：

```markdown
## 直接结论
```

在该小节内：

1. **第一句** 即给出可识别答案：优先 **是 / 否 / 可以 / 不可以 / 视条件：…** 等；避免用「一般来说」「可能」开头糊弄重点问句。
2. 若存在 **前提或例外**，用 **不超过 3 条** 短列表列在结论句下方；**不要把结论只写在列表前的长段落里**。
3. 若答案依赖用户尚未提供的信息，结论句写 **「当前信息不足，无法判定」**，并 **单独一行** 列出还缺什么。

### 6.3 禁止怎么做

- **禁止** 把用户最关心的 **是/否或可/不可** 只写在中间大段分析里，且总结处没有 **「## 直接结论」** 块。
- **禁止** 用「见上文」代替结论块；用户应 **不滚动翻找** 也能在总结区看到答案。
- **禁止** 用「完成了哪些改动」代替 **§6.6** 要求的：对用户问题的答复、验证项、根因依据、改后再验（或未验说明）。

### 6.4 与分析过程的分工

- **分析过程**：推理、引用代码路径、罗列步骤——可放在「直接结论」**之前**。
- **直接结论**：只承担 **可扫描的最终答案**；与本文 **§5、§7**、本仓库其他规则并存时，**结论块不得省略**（§7 不因「直接结论」而允许用错误展示掩盖根因）。

### 6.5 与其它规则的关系

- 本规则 **不替代** `ai-data-integrity.mdc`、`architecture.mdc` 等；若结论涉及「不能做」，须在结论或紧随列表中点明 **受限原因**（一条即可）。
- 在本文 **§1【本次使用规则】** 中，若本轮触发了重点问答，应列出 **`rule-execution-declaration.mdc`（§6 直接结论）**。

### 6.6 结论须围绕用户问题（禁止以利迭为主轴）

适用于：用户问的是 **现象、根因、对不对、修没修好、责任在哪一层** 等；以及任何需要 **`## 直接结论`** 的回复。

在 **`## 直接结论`**（或语义等价的总结块）中，除 **§6.2** 对「是/否」的要求外，还须满足：

1. **主轴对准用户**：先用一两句话概括 **用户到底在问什么、结论是否回答了那一问**。**禁止**把「改了哪些文件 / 加了哪些接口」当作结论的唯一或首要内容；实现说明可放在结论**之前**的分析段落，不得顶替「对用户问题的答案」。
2. **写明验证了什么**：列出本轮**实际做过**的验证（例如：引用了哪段代码证明某条 SQL 未更新某列；执行了哪条查询/脚本、哪个样本 `task_id` 的前后字段；跟了哪段日志 tag）。**仅编译通过**、**仅在代码里存在调用** **不得**表述为「已证明线上/本机数据问题消失」。
3. **证明问题在哪**：用简短语句给出 **根因落点**（哪一层、哪条路径、哪个条件），并点明 **依据类型**：代码 / 日志 / 数据对比中的**至少一类**（见 `verify-before-change.mdc` §1）。
4. **修改后的再验证**：若本轮**合并了代码或配置变更**，须说明 **改后** 用何种事实、得到什么结果（与改前同一维度对比更佳）。若**未做**运行时、数据库或接口再验，须在结论中 **单独明确**「未完成再验」及原因（如无库、无环境）。**禁止**用推测代替再验结果。

与 `verify-before-change.mdc` 的分工：该文件约束 **何时允许改**；本节约束 **结论怎么写**。二者同时适用时，未取证即改动仍属违规；结论里也不得隐瞒「未验证」。

---

## 7. 问题排查与改动边界（全工程永久有效）

### 7.1 先查根因，禁止只改「显示结果」

- 用户反馈**现象异常**（例如「为何显示 1+2=5」「界面显示错了」）时，**必须先分析成因**：数据来源是否错、中间运算/业务逻辑是否错、状态同步是否错、接口/缓存是否错等。
- **禁止**在未定位根因前，为「看起来对」而**直接改展示层**（例如把 `5` 改成 `3`、硬编码覆盖文案），掩盖真实错误。
- 若根因暂不可证，须说明**已排除什么、仍怀疑什么、建议下一步如何验证**，而不是先改显示糊弄过去。
- **排查顺序**（与 `root-cause-before-display-fix.mdc` 一致）：先核实**读展示/绑定** → 再核实**读到的数据与数据源** → 若均正确再查**写入/同步是否成功**；**禁止**跳过链路只改 UI。

### 7.2 最小改动；能不改则不改

- 解决问题须 **最小必要 diff**：只改与根因直接相关的代码或配置。
- 若**不改代码**即可解决（配置、操作步骤、环境、数据修正说明等），则**不要改代码**。
- **禁止**顺手重构、格式化大段无关文件、扩大需求范围式「瞎改」。

### 7.3 禁止未经要求的兜底、回退类功能

- 用户**没有明确要求**做 **兜底**、**回退**、**撤销恢复**、**自动降级** 等能力时，**禁止**自行加此类功能（含 UI、状态机、额外存储、隐藏分支逻辑）。
- 若认为有必要，须**先征得用户同意**再实现；不得默认「为你好」加一套。

---

## 8. 删除指令：彻底移除、禁止兼容层（全工程永久有效）

当用户明确要求 **删除 / 去掉 / 移除 / 废弃** 某项内容，或表述为 **「不要做任何兼容」「像从未加入过一样」「直接删干净」** 等语义时，须按本节执行；**本节优先于**「为旧数据留口子」的惯性做法。

### 8.1 必须做的事（全链路清零）

- **代码**：从 **CREATE/ALTER、INSERT/UPDATE/SELECT、DTO、归一化、QML/C++ 绑定、云请求/响应解析、测试与注释** 中移除该内容；用全仓搜索（标识符、字符串键、camelCase/snake_case）**搜到零相关引用**（允许的例外见 **8.3**）。
- **文档与契约**：`docs/`、`.cursor/rules/`、产品契约中与该内容相关的 **表格行、附录、示例、勾选说明** 一并删除或改写到 **不再出现该概念**。
- **目标状态**：阅读代码与文档时，**看不出**曾经存在过该字段/能力（除非 **8.3** 允许的残留说明）。

### 8.2 禁止做的事（一律视为违反本节）

- **禁止**为「平滑过渡」保留：双写、双读、旧键回落、`#if`/`feature flag` 并行分支、deprecated 包装函数、「暂时仍解析旧 JSON」等 **兼容层**。
- **禁止**未经用户**单独明确要求**而主动：编写 **数据迁移**、**DROP COLUMN**、**回填脚本**、**版本门控** 等；用户若只说「删除」而未说要迁历史库，则 **不做** 这类处理。
- **禁止**在回复中把「兼容旧库/旧云文档」当作默认方案；若技术上有物理残留（如用户本机 SQLite 里仍有空列），仅在回复中 **一句客观说明**即可，**不得在代码里为残留列维护读写路径**。

### 8.3 允许的残留（仅限下列）

- **安全/数据完整性**：例如从 **`userMeta` 等用户可控 JSON** 中 **剔除**已废弃键名，防止注入冒充系统字段（属于 **清理**，不是「兼容读取业务字段」）。
- **用户另行授权**：用户**明确**要求「只做迁移 / 只保留读取旧数据 N 个版本」时，按用户给出的范围实现，并在回复中声明 **依据用户例外授权**，与本节默认口径区分。

### 8.4 与 §7 的关系

- §7 强调 **最小改动**；本节强调 **删除类任务** 的改动范围是 **所有相关引用**，而不是「最小保留兼容」。二者同时适用时：**删除指令以 §8 为准**。

### 8.5 执行披露

- 凡执行 **§8** 类任务，须在 **§5** 的执行披露中写明：**rule-execution-declaration.mdc §8（删除=彻底移除）**，并列出已清理的主要区域（如 Storage / Repository / QML / 契约）。
