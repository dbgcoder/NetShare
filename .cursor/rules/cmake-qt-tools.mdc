---
description: Windows 上本仓库构建须使用 Qt 自带的 CMake 路径 + MSVC 工具链与 Qt msvc2022_64 套件；禁止默认使用 MinGW。
alwaysApply: true
---

# CMake 与编译器（Windows · 本机固定）

## 1. CMake 可执行文件路径

- **CMake bin 目录**（用户指定）：`C:\Qt\Tools\CMake_64\bin`
- **调用方式**：本会话 `PATH` 最前追加该目录，或直接使用  
  `C:\Qt\Tools\CMake_64\bin\cmake.exe`。

- **禁止**：在未尝试上述路径的情况下，以「环境没有 cmake」为由跳过构建。

## 2. 必须使用 MSVC，禁止使用 MinGW（除非用户明确要求）

- **本工程桌面端默认**：**MSVC** + Qt **`msvc2022_64`** 套件（与仓库 `find_package(Qt6 6.8.3)` 一致）。
- **推荐 `CMAKE_PREFIX_PATH`**（本机 Qt 安装，按项目 `CMakeLists.txt`）：  
  `C:/Qt/6.8.3/msvc2022_64`
- **禁止**：AI 在「用户未明确要求 MinGW」时，使用 **`MinGW Makefiles`**、**`mingw_64`** 作为配置/构建生成器或 Qt 套件，以免与既有 MSVC 产物、调试习惯不一致。
- **MinGW** 若必须使用时，须使用**独立**构建目录（例如 `build-mingw`），**不得**与下文 **MSVC/Qt 共用** 的 `build` 混用。

## 2.1 统一构建目录（源码编译 · 与 Qt 共用）

- **固定路径**：**`D:\FXduan\AiModelDemo\build`**
- **含义**：本仓库**日常开发、AI 终端 `cmake --build`、Qt Creator / Qt Kit** 打开工程时，**共用同一 CMake 构建树**（`-B` 指向上述目录），避免 `build-msvc`、`Desktop_Qt_*` 等多套目录并存导致混淆。
- **与打包目录区分**：**`Build-pack\aimodel-pack`** 仅用于 **Windows 安装包/绿色版** 相关 CMake 目标（如 `aimodel-pack-all`），**不**替代 `D:\FXduan\AiModelDemo\build` 作为日常编源码的默认目录。

## 3. 推荐配置与构建（Visual Studio 2022 + x64）

**首次配置**（源码在 `D:\FXduan\AiModelDemo`，`-B` 使用统一 `build`）：

```powershell
$cmake = "C:\Qt\Tools\CMake_64\bin\cmake.exe"
& $cmake -S "D:\FXduan\AiModelDemo" -B "D:\FXduan\AiModelDemo\build" `
  -G "Visual Studio 17 2022" -A x64 `
  "-DCMAKE_PREFIX_PATH=C:/Qt/6.8.3/msvc2022_64"
```

**编译 Release**：

```powershell
& "C:\Qt\Tools\CMake_64\bin\cmake.exe" --build "D:\FXduan\AiModelDemo\build" --config Release
```

验证配置是否走 MSVC：配置日志中应出现 **`CXX compiler identification is MSVC`** 与 **`cl.exe`**。

## 4. Ninja + MSVC（备选）

仅当用户明确要求或环境不适配 VS 生成器时：在已执行 **`vcvars64.bat`** 的 shell 中，使用 **`cl`** 作为编译器，`-G Ninja`，**仍须** `CMAKE_PREFIX_PATH` 指向 **`msvc2022_64`**，**不得**指向 `mingw_64`。

## 5. AI 执行策略小结

- 需要在本仓库「自己编译」时：**主动**使用 §3 的 `cmake.exe` 路径 + **MSVC** 配置，**`-B` 默认** **`D:\FXduan\AiModelDemo\build`**（与 Qt Creator 共用）；成功后可在回复中简短说明路径（如 **`build\Release\AIModel.exe`**，相对仓库根）。
- 与 `cloudfunc-auto-package.mdc` 中的 **Go** 路径约定独立，不互相替代。
