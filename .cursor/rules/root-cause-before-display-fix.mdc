---
description: 所有问题先查根因链路（读→源→写），禁止只改界面展示糊弄正确结果。
alwaysApply: true
---

# 根因优先 · 禁止「只改显示」糊弄（全工程永久有效）

## 1. 适用范围

- **所有提问与排障**：凡涉及「界面上不对 / 显示错了 / 和预期不一致」——包括但不限于任务 ID、用户名、时间、状态、列表条数、云端与本地不一致等。
- **不视为仅显示问题**：若根因在数据源、写库、网络、云函数、权限，**必须**先定位到该层，**禁止**在未定位前用 UI 层「对齐」假象。

## 2. 强制排查顺序（由近到远）

在改代码前，按顺序**核实**（可用日志、断点、数据库、网络回包，而非臆测）：

1. **读展示层**：绑定字段、格式化、排序、过滤、分页是否把**正确数据**显示错？（若此处错，最小改动修绑定/格式，并说明为何不是下游问题。）
2. **读链路 / 读到的数据**：Repository、SQL、QML 读模型、云端 list 解析——**实际读到的值**是什么？是否与上一层期望一致？
3. **数据源头（当前事实源）**：内存模型、本地 SQLite、云端集合——**源里存的**是否已是错的？若源已错，**展示层改对仍是造假**。
4. **写源头**：谁写入/更新了该源？写入是否成功（HTTP/云函数返回、事务、冲突）？失败时**不得**用显示层掩盖。

**示例（任务 ID 显示错误）**：先查列表/详情**如何读** `taskId` → 再查读到的**行对象/SQL 行**是否已是正确 ID → 若读法与源数据都对，再查 **submit/upsert/同步** 是否写成功。

## 3. 禁止行为

- **禁止**在未证明「仅是展示绑定错误」时，只改 `Text`/`Label` 的拼接、截断、兜底文案、硬编码覆盖，使界面「看起来对」。
- **禁止**用「先让你看见对的」之类说辞回避写库/同步失败。
- **禁止**把唯一结论藏在长篇分析里却不说明**失败发生在读、源、还是写**（重点问句仍须遵守 `rule-execution-declaration.mdc` §6「## 直接结论」）。

## 4. 与现有规则的关系

- **`rule-execution-declaration.mdc` §7**：先查根因，**禁止**只改显示层糊弄正确结果；本节将同样原则**具体化为可读→源→写顺序**，并覆盖「所有提问」。
- **`ui-changes-consent.mdc`**：若修复方案需要**新增**可见控件/标题，仍须征得用户同意；**修正错误绑定**（不改变信息架构）一般不受「新增按钮/标题」限制，但须符合本节**根因优先**。
- **`ai-data-integrity.mdc`**：写库与展示须与时间、模型结果等约束一致，不得以展示修改破坏数据语义。

## 5. 不符合时

若因信息不足无法完成某一层的核实，须明确写出：**已排除什么、仍缺什么证据、建议下一步如何取证**；**不得**用改显示代替取证。

## 6. 何时允许改代码

定位到层之后，**何时动刀**见 **`verify-before-change.mdc`**：先验证假设成立，再最小改动；改后再验。
