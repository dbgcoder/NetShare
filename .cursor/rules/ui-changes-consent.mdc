---
description: 未经用户明确同意，禁止在界面擅自新增标题、按钮等可见控件；需先征得同意或仅给出方案待批。
alwaysApply: true
---

# 界面改动须用户同意（全工程永久有效）

## 1. 适用范围

- **Qt/QML 界面**：`src/ui/**/*.qml`、`src/app/**/*.qml` 等所有用户可见页面、弹窗、导航、工具栏、表头、侧栏。
- **“界面”包括**：新增或改文案的**标题/副标题/分区标题**、**Button**、**ToolButton**、**Tab**、表**列头**、独立**面板/卡片标题**等会改变布局或信息架构的元素。
- **不视为本规则限制的**：纯逻辑/C++、样式微调（颜色间距且不改结构）、**用户当条消息明确要求**的控件与文案、**删除**用户要求去掉的内容。

## 2. 禁止行为

在用户**未在本轮对话中明确同意**（例如「可以加上去」「按方案加按钮」）的情况下，**禁止**：

- 新增 **Button / ToolButton / 链接式可点控件**（含菜单项、浮动条上的新入口）。
- 新增 **标题类文案**（`Text`/`Label` 作为区块标题、新列头、新页面大标题等），若会改变用户已习惯的版面层次。
- 为「方便调试/提示」在**软件窗口内**加可见说明或面板（调试输出仍遵守 `debug-logging.mdc`，仅 Qt Creator 控制台）。

## 3. 必须怎么做

- 若实现任务**确实需要**新按钮或新标题：在改代码前**先说明要加什么、放哪、用途**，并**等待用户明确同意**；或拆成两步：本轮只出文字方案，下一轮用户同意后再改 QML。
- 用户仅要求「修 bug / 删东西 / 对齐现有 UI」时：**只改与请求直接相关的部分**，不顺带「优化」加控件。

## 4. 与其它规则的关系

- **`architecture.mdc`**：界面职责仍在 QML；本规则约束的是**能否新增可见结构**，不改变 QML/C++ 分工。
- **`rule-execution-declaration.mdc`**：执行任务时若在 §1「本次使用规则」中涉及 QML 界面修改，若会新增标题/按钮，须列出 **`ui-changes-consent.mdc`**，并确认用户已同意或本次属于用户明确要求/删除类任务。

## 5. 不符合时

若技术上必须新增控件且无法等待用户回复：在回复中**明确写出**违反了本规则哪一条、原因、以及建议用户事后确认是否保留；**禁止**不说明就加。
