---
description: 崩溃安全工作流：细分步骤、每步立即保存、异常后从断点继续。全工程永久强制执行。
alwaysApply: true
---

# 崩溃安全执行规则（全工程永久有效）

本项目 IDE 频繁异常关闭，**所有任务必须按以下规则执行，不得例外。**

## 1. 任务拆分：每个问题必须细分步骤

- 收到任务后，第一件事是拆成可独立完成的小步骤（每步 ≤ 1~2 次代码修改）。
- 步骤列表写入 `docs/*-analysis.md`，用勾选清单格式。
- 禁止"一口气改完再保存"，必须每步改完立即保存。

## 2. 每步完成立即保存（代码 + 文档双保存）

- **代码保存**：每完成一个步骤的代码修改，立即更新对应文件（StrReplace/Write 完成即视为保存）。
- **文档保存**：每完成一个步骤，立即更新 `docs/*-analysis.md`：
  - 勾选已完成步骤 `[x]`
  - 更新"当前断点"为下一步
  - 记录本步改了什么、为什么、影响范围
- **工具操作频率**：最多每 1~2 次工具操作就落盘一次阶段性结论，不得只在脑内推演。

## 3. 异常退出后从断点恢复

- 用户说"继续"时，固定执行恢复流程：
  1. 读取最近更新的 `docs/*-analysis.md`
  2. 提取"当前断点 / 下一步操作"
  3. 从断点继续，不重复已完成步骤
- 若重跑某步，必须在文档中注明"重跑原因"。
- **仅在用户明确要求继续时恢复旧任务**：触发词包括「继续」「接着上次」「从断点继续」。
- **若用户未明确说继续**（即使软件刚异常重启），必须把用户当前消息视为新任务：
  - 直接回答/执行新问题
  - 不主动读取旧断点、不回看老问题、不自动重跑上个任务
  - 仅在用户随后明确要求继续时才进入恢复流程

## 4. 数据实时持久化（代码层面）

- 分镜/人物/物品/场景等数据的任何新建、删除、编辑操作，必须**立即调用持久化函数**写入数据库，不依赖定时器。
- `queueStoryboardAutoSave()` 已改为立即同步保存（双 key），禁止改回延迟保存。
- 模型回填完成后，必须立即用快照数据调用 `persistStoryboardSnapshotForProject`，不走 `_currentStoryboardRowsAsArray()`。
- 切页/关闭时如果回填已持久化，不再二次保存（防覆盖）。

## 5. 分析结果必须落盘

- 分析结果不得只留在对话里，必须写入 `docs/` 文件。
- 每个问题至少维护一个 `docs/*-analysis.md`，包含：
  - 问题现象
  - 根因分析
  - 修复步骤（勾选清单）
  - 当前断点
  - 下一步操作
  - 更新时间
- 若发现新问题分支，先在原文档补"分支问题"与"切换原因"，再开始新分支。

## 6. 恢复标准动作

收到"继续"后：
1. 读取最近更新的 `docs/*-analysis.md`
2. 提取"当前断点 / 下一步操作 / 阻塞点"
3. 先更新文档（写入本轮开始时间与计划），再改代码
4. 每完成一步，立刻回写文档并更新勾选状态

发生重启但用户未说"继续"：
1. 不做恢复动作
2. 以当前用户新问题为唯一输入直接处理
