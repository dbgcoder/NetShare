---
description: 按事实改代码；每一步先验证是根因再动刀；无验证不改；改后须再验证。
alwaysApply: true
---

# 取证后再改 · 改后再验（全工程永久有效）

与 `root-cause-before-display-fix.mdc`（读→源→写顺序）配合：**顺序用于定位层，本条用于何时允许改代码**。

## 1. 允许的「事实」来源（至少命中其一再视为已验证）

- 本机 **SQLite / 日志** 中与问题行一致的数据；
- **网络回包**、云函数返回、控制台按 `debug-logging.mdc` 的 tag；
- 调试器 **断点** 下看到的变量/调用栈；
- 用户 **明确提供** 的可复现步骤 + 与步骤一致的观测结果。

**不算已验证**：仅「读代码觉得像」「理论上可能」「先改一版试试」。

## 2. 每一步改动必须满足的格式

对**每一处**拟修改，在动手前写清（可在回复或 `docs/*-analysis.md`）：

1. **现象与假设**：当前怀疑哪一层、哪条路径；
2. **验证动作**：准备执行什么查询/日志/对比，**期望若假设成立应出现什么**；
3. **验证结果**：实际结果是否满足期望；
4. **仅当 3 为是**，才写该步的代码 diff；否则**不改**，并写明**排除/仍怀疑**什么。

禁止跳过 2～3 直接改代码（用户当条消息**明确要求先试补丁**的除外，须在披露中写明依据用户例外）。

## 3. 改后验证（对错）

- 改动合并后，用**同一类事实**再验一次：例如修写库则再查库或看请求体；修展示则对照绑定与源字段。
- 若无法自动跑通，须写明**用户侧如何一眼确认**（操作步骤 + 期望现象）。
- 回复中的 **`## 直接结论`** 须同时遵守 `rule-execution-declaration.mdc` **§6.6**（围绕用户问题、写明验证了什么、根因依据、改后再验结果或未验原因）。

## 4. 不符合时

若环境不允许取证（无库、无运行），须写明：**未验证、未改代码**，并列出**仍缺什么证据**；**禁止**用「先改再说」代替。

## 5. 与 § 其它规则关系

- `rule-execution-declaration.mdc` §7 **最小改动**：在通过本节验证的前提下，改动范围仍须最小。
- `crash-safe-workflow.mdc`：多步排障时，验证结果与断点写入 `docs/*-analysis.md`。
