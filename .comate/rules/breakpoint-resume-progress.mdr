---
description: 大文件断点续传和移动端进度显示的开发规范
globs: src/network/StreamingMultipartParser.*,src/network/RequestHandler.*,src/network/WebSocketHandler.*,src/main.cpp,web/upload.html
alwaysApply: false
---

# 大文件断点续传与移动端进度规范

## 核心原则

1. **续传优先**：任何上传中断都应保留部分文件，支持下次续传
2. **双源进度**：XHR upload.onprogress（桌面端精确）+ WebSocket transfer_update（移动端兜底）
3. **确定性路径**：续传文件必须使用与原始上传相同的路径，不使用冲突解决重命名

## 服务端规范

### StreamingMultipartParser 续传
- `setResumeOffset(qint64)` + `setResumeFilePath(const QString&)` 设置续传参数
- 续传模式：`QIODevice::WriteOnly | QIODevice::Append`，禁止 WriteOnly 单独使用（会截断）
- 续传时跳过冲突解决循环（不生成 `file (1).ext`）
- `m_currentFileBytesWritten` 初始化为 `m_resumeOffset`，保证进度正确
- 打开文件前验证 `QFileInfo(path).size() == resumeOffset`，不一致则回退到普通模式

### handleUploadCheck 部分文件检测
- 完整匹配：`fi.size() == expectedSize` → `existing` 数组
- 部分匹配：`0 < fi.size() < expectedSize` → `partial` 数组，包含 `{"path", "size"}`
- `transferredSize` = 已完整文件大小 + 部分文件偏移量之和

### handleUploadSingleFile 续传头
- `X-Resume-Offset`：续传偏移字节数
- `X-Resume-Path`：续传文件的相对路径
- 验证 session 存在且路径在文件列表中
- 验证磁盘文件大小 == offset（安全检查）

### WebSocket 双 token 广播
- 上传任务同时映射到 sessionId 和 shareToken
- `m_taskToToken[taskId]` = sessionId（上传页面订阅）
- `m_taskToShareToken[taskId]` = shareToken（下载/分享页面订阅，可能为空）
- `main.cpp` 广播时检查两个映射，都广播

### 资源清理
- session 移除时同步清理 `m_taskToToken` 和 `m_taskToShareToken`
- Socket 断开时：
  - 标记 FileTransferEngine 任务为失败（`failTask`）
  - 清理 `m_taskToToken` 和 `m_taskToShareToken` 映射
  - 删除 parser 和 state，关闭文件句柄
  - **移除 UploadSession**（不保留），让客户端通过 `/api/upload/check` 重新创建会话
  - 部分文件保留在磁盘，供下次 `/api/upload/check` 检测

### 客户端重试流程
- `retryFailed()` 必须重新调用 `/api/upload/check` 获取新 session 和最新 partial 信息
- 不使用旧 sessionId 重试（旧 session 可能已被清理）

## 客户端规范

### 断点续传逻辑
- 从 `/api/upload/check` 响应获取 `partial` 数组
- 使用 `file.slice(offset)` 创建剩余部分的 Blob
- XHR 请求添加 `X-Resume-Offset` 和 `X-Resume-Path` 头
- 显示进度时：实际进度 = offset + e.loaded

### beforeunload 处理
- 页面关闭/刷新时 **不调用** `abortUpload()`
- 保留部分文件供下次续传
- 仅用户主动取消时调用 `abortUpload()`

### WebSocket 进度
- 从 `/api/upload/check` 获取 sessionId 后建立 WebSocket 连接
- 订阅 sessionId 接收 `transfer_update` 消息
- 双源进度：优先 XHR（更精确），XHR 无事件时使用 WebSocket
- 显示连接状态指示器

### 移动端兼容
- WebSocket 端口 = location.port ? parseInt(location.port) + 1 : 8081
- 断线自动重连（5秒间隔）
- 后台切换时 WebSocket 可能断开，重连后重新订阅
