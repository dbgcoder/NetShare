---
description: 全工程统一调试与日志规则（Qt Creator Application Output、格式、任务页/密钥链路打点、脱敏）。合并自原 qtcreator-debug-output.mdc。
alwaysApply: true
---

# 调试与日志规则（全工程永久有效 · 单一文件）

## 0. 范围说明

- **原独立「调试规则」文件数量**：**1 个**（`qtcreator-debug-output.mdc`），已**全文并入本文件**后删除旧文件，避免重复与分叉。
- **非调试类规则**（架构、数据完整性、崩溃安全工作流、云函数打包等）**不合并进本文件**，仍在各自 `.mdc` 中；**执行披露**见 `rule-execution-declaration.mdc` **§5**。涉及 AI 回填/原文时，日志与展示须同时遵守 `ai-data-integrity.mdc`。
- **触发本规则**：用户要求「加完整调试信息 / 控制台看日志 / 定位读库-排序-显示 / 链路耗时」等。

---

## 1. 输出渠道（仅 Qt Creator IDE，不在软件内展示）

- **加完整调试信息时**：只在 Qt Creator 的 **Application Output** 输出，**不要在软件界面内加调试输出**（如页面内调试窗口、调试面板、Text+ScrollView 等）。
- **QML 控制台**：用 `console.log(...)` 或 `console.warn(...)` 输出，在 Qt Creator Application Output 可见。
- **喵窗拖放诊断（QML）**：`[MeowDockDrop]`、`[MeowDockDnd]`、`[WMContent]`（Loader/ListView 注册）、`[EntityPanel][char|entity].refDrop|resultDrop` 等由根上下文 `aimodelVerboseLog` 门控；**仅当**启动前环境变量 **`AIMODEL_VERBOSE_LOG`** 为 **`1` / `true` / `yes` / `on`**（不区分大小写）时输出（与 C++ `qDebug/qInfo` 开关一致）。默认静默，避免日常刷屏。
  - **工作台 → 喵窗（固定条）**：tag 以 **`projdock.`**（`MeowDockDrop`）或 **`strip.`**（`MeowDockDnd`）开头时，**不经** `aimodelVerboseLog` 门控，由根上下文 **`aimodelDiag.warn`**（`AimodelQmlDiag`，C++ **`qWarning`**) 写出，**默认即可在 Qt Creator Application Output 与 `qml_error.log` 中看到**；含 `projdock.ref.*` / `projdock.sb.*` / `projdock.import.*` / `projdock.strip.drop` / **`projdock.manual_pin`** / **`projdock.manual_pin.probe`**（释放时命中探测）/ **`projdock.manual_pin.skip`** / `strip.boot|enter|exit|drop.*` / **`strip.collapsed.*`** 等。坐标优先 **`aimodelDiag.cursorGlobalPos()`**（`QCursor::pos`），避免源 `MouseArea` 上 `mapToGlobal(mouse)` 在指针已移出控件时偏差。
- **C++ 控制台默认级别**：后端调试日志**默认使用 `qWarning()`**（本工程 `main.cpp` 默认会过滤 `QtDebug/QtInfo`，除非手动设置 `AIMODEL_VERBOSE_LOG=1`）。
- **仅在明确开启 verbose 时**：才可额外输出 `qInfo()` / `qDebug()`。
- **禁止**：在页面内增加调试日志展示区域、调试窗口、调试面板等 UI 元素。

### 1.1 永久执行策略（无需重复提醒）

- 以后用户只要提「加调试信息 / 看控制台耗时 / 定位慢点」，**直接按本规则落地**，不再反复提醒「需要改成 qWarning 才看得到」。
- 默认目标是：用户在 Qt Creator 的 Application Output 里**立即可见**完整调试链路。

---

## 2. 日志格式（必须统一）

- 统一前缀：`[ISO时间][模块][tag] message`
- 例如：
  - `[2026-03-18T16:40:01.123Z][TaskRecordDebug][load.sort.before] ...`
  - `[2026-03-18T16:40:01.456Z][StoryboardStorage][allocateTaskId] ...`
- **`*` 占位**：若某行在拼接前尚未取到 ISO 时间，可暂用 `[*]`，随后在同一次调用链的后续日志中补全 ISO（与 §7 QML 示例一致）。

---

## 3. 任务记录页必打日志点（排序问题专用）

- `load.begin / load.end`
- `load.sort.before / load.sort.after`（QML `TaskRecordPage.qml`：`[*][TaskRecordDebug][load.sort.before|after]`，含 `completedAt_len` / `sortTs` / `resourceId_len`；与 C++ `TaskRecordService::rowSortTimestamp` 一致）
- `refresh.running.begin / refresh.running.end`
- `refresh.running.db`（逐条打印 `taskId/status/createdAt/updatedAt/cTs/uTs`）
- `refresh.running.insert / refresh.running.update`
- `reorder.before / reorder.after`

每条样本至少包含：

- `taskId`
- `status`
- `createdAt`
- `updatedAt`
- 解析后的时间戳（`cTs/uTs/sortTs`）

---

## 4. Qt Creator 可见性要求

- 用户使用 Qt Creator 时，调试信息必须能在 **Application Output** 直接看到。
- 不允许只写本地文件而不打控制台。
- **禁止**在软件界面内增加调试窗口、调试面板或日志展示区域；调试输出**仅**在 Qt Creator IDE 中查看。

---

## 5. 复盘记录（任务页 / QML 模型）

### 5.0 ListModel / QObject（2026-03-18）

- **现象**：任务页自动刷新后顺序异常，且日志持续刷 `TypeError: QObject has no hasOwnProperty function`。
- **根因**：`ListModel.get(i)` 返回 `QObject`，不能按普通 JS 对象使用 `hasOwnProperty`。
- **修复**：改为显式字段拷贝函数（如 `_cloneTaskRow`），禁止对 `QObject` 用 `hasOwnProperty` 做 clone。
- **结论**：QML 模型对象处理统一采用「显式字段拷贝」策略，避免刷新期对象失效与异常刷屏。

### 5.1 任务排序问题（时序 / 时区）

- **现象**：新任务不在最上方，跨数据源（image_history/project_task）排序错乱。
- **根因**：SQLite `datetime('now')` 为 UTC，QML 本地墙钟若混进同一排序列会错位；另需统一 **Qt 格式 `HH`（24h）** 与 `datetime('now', 'localtime')` 写入。
- **修复**：业务 TEXT 时间列用 `datetime('now', 'localtime')`；可读字符串 **`yyyy-MM-dd HH:mm:ss`**；任务事实源以 **`jobs`** 为准（见 `docs/timezone-and-creation-time-spec.md`）。

---

## 6. 执行要求

- 新增调试时，必须先写控制台日志，再补文件落盘。
- 若为 C++ 链路，默认一律用 `qWarning().noquote()` 输出完整文本（含 request/response/时间戳）。
- **密钥类字段**：允许打全链路阶段日志，但 **HTTP 体中的 token / sessionToken / API Key 须在日志中脱敏**（仅打印长度或 `<redacted len=N>`），禁止在 Application Output 输出明文密钥。
- 修复完成后，必须回报：
  - 日志在哪看（Qt Creator Application Output / 文件路径）
  - 本次新增了哪些 tag
  - 下一次复用时的调用入口（**本文件** `.cursor/rules/debug-logging.mdc`）

---

## 7. 提供商密钥链路（ProviderKeys / CloudAuth）

用户排查「保存 / 上传 / 返回 / 下载 / 显示」时，控制台应能串起以下 tag（时间在方括号内为 UTC ISO，与规则 §2 一致）：

**QML（`SystemManagePage.qml`，`console.warn`）**

- `[*][ProviderKeys][save.ui]` — 点击保存前：所选 `providerTargetUser`、解析得到的 `ownerUserId`、ComboBox 索引/文本、选项列表摘要、密钥长度。
- `[*][ProviderKeys][local_db.write]` — 写入本地规则库（ProjectRuleService）中的密钥行。
- `[*][ProviderKeys][save.cloud_immediate]` — 保存密钥按钮：**不入批量队列**，本地写库后直接 `saveProviderKeys` 上传（残留 `_pendingCloudKeySync` 会清空，避免后续 flush 重复 POST）。
- `[*][ProviderKeys][reload.skip_local_prefetch]` / `[load.skip_during_cloud_reload]` — 「重新加载」：**不先读本地**，对全部高级用户串行 `provider_keys_get`，写回本地后再 `loadProviderKeys` 刷新当前用户界面；`sync.plan` 中 `mode=cloud_reload_all`。
- `[*][ProviderKeys][cloud.dispatch]` — 实际发起 `type=key` 或 `type=settings_batch`，含 `owner` + `ownerUserId` + 长度。
- `[*][ProviderKeys][load.local_db]` — 从本地读出的密钥长度。
- `[*][ProviderKeys][load.cloud_fetch]` — 将调用 `provider_keys_get`（本地无密钥且未拉取过）。
- `[*][ProviderKeys][display.cloud_payload]` — 收到 `providerKeysLoaded`：pending owner、信号用户名、当前选中用户、`hasKeys`、长度。
- `[*][ProviderKeys][display.ui_apply]` — 已把结果应用到当前选中用户的输入框（掩码显示）。
- `[*][ProviderKeys][cloud.save_ok]` / `[cloud.save_fail]` — 云端保存信号结果。

**C++（`CloudAuthService.cpp`，`qWarning().noquote()`）**

- `[ISO][CloudAuth][provider_keys_get.begin]` — 发起下载前环境与参数摘要。
- `[CloudAuth][provider_keys_get][ts=…]` — HTTP、`req_redacted`、`resp`（响应中的密钥若云端返回则仍可能出现在 `resp` 行，敏感环境可再收紧）。
- `[ISO][CloudAuth][provider_keys_get.display_emit]` — 解析完成后即将 `emit providerKeysLoaded` 的长度摘要。
- `[ISO][CloudAuth][provider_keys_set.begin]` — 保存前摘要（含 `ownerUserId`）。
- `[ISO][CloudAuth][provider_keys_set.upload]` — 即将 POST，`req_redacted` 含脱敏后的完整 JSON 结构。
- `[CloudAuth][provider_keys_set][ts=…]` — 响应原文与 `done` 行。
- `[ISO][CloudAuth][settings_batch_set.begin]` / `upload` — 批量规则+密钥上传（请求体脱敏）。

**一致性要求**：`provider_keys_set` 与 `provider_keys_get` 必须使用 **同一套** `ownerUsername` + `ownerUserId`（`userId` / `targetUserId`），否则会出现「保存成功但拉取仍为空」。

---

## 8. 与其它规则的衔接（不重复收录正文）

| 规则文件 | 与本调试规则的关系 |
|----------|---------------------|
| `rule-execution-declaration.mdc` | AI 执行任务时声明是否用到本调试规则；**§5** 为执行披露（原独立文件已并入） |
| `ai-data-integrity.mdc` | 控制台调试不得违背原文不改、模型结果落库等要求；**§7** 为时间字段统一 |
| `architecture.mdc` | 业务逻辑仍在 C++；调试输出可在 QML `console` 或 C++ `qWarning` |

---

## 9. 页面加载失败（Loader.Error）必打点

当出现「页面加载失败」或 `Loader.Error` 时，必须在 **Qt Creator Application Output** 输出完整字段，禁止只打印一行泛化错误：

- 固定 tag：`[PageLoaderDebug][error]`
- 必含字段：
  - `index`（导航页索引）
  - `source`（如 `pages/SystemManagePage.qml`）
  - `active`
  - `status`
  - `isCurrent`
  - `shouldLoad`
  - `err`（`errorString`；若为空需明确打印 `(empty errorString)`）

推荐格式（QML `console.error`）：

- `[PageLoaderDebug][error] index=... source=... active=... status=... isCurrent=... shouldLoad=... err=...`

若 `err` 为空，优先检查：

1. QML 类型未注册/导入路径错误（`Xxx is not a type`）
2. `qt_add_qml_module` 的 `QML_FILES` 未包含新增文件
3. 资源路径相对层级错误（尤其是 `import "../..."` / `import "../../..."`）

---

## 10. 登录后云端同步（模型配置页 / 项目规则 / 密钥）

**QML（`main.qml`，`console.warn`）** — 固定模块名 **`LoginSync`**，时间在方括号内为 UTC ISO（与 §2 一致）：

- `[LoginSync][login.session.begin]` — 登录成功瞬间：`user`、`role`、`keyOwner`、`tokenLen`、`cloudAuthEnabled`、`cloudSessionActive`。
- `[LoginSync][integrity.owner_pull_dispatch]` — 即将按 `siteOwner` 拉项目规则 + 模型目录 + 软件配置：`normUser`、`user`、`role`、`keyOwner`、本地是否已有规则行/目录快照（`local.rules.site.nonEmpty`、`local.catalog.nonEmpty`）。
- `[LoginSync][integrity.snapshot]` — 本地库完整性快照（可多次，`phase=` `post_login_immediate` / `post_login_settled_2s5` / `after_project_rules_cloud_persist` / `after_model_catalog_cloud_persist`）：`siteOwner`、`table.default_priority_json.len`、`effective_priority_json.len`（目录内 `defaultPriority` 或回落表）、`defaultTextP1` / `defaultImageP1`、`effective_priority.preview180`、`catalog.json.len`、`catVer`、`contentHash.len`、`catalog.syncedAt`、按 `siteOwner` 的 `project_rules_by_owner` 各字段**长度**（`tr`/`chr`/…，**不打规则全文**）、`catalog_resolver.ready`。
- `[LoginSync][integrity.cloud_project_rules]` — 云端 `project_rules_get` 落到 QML 后、写本地前：`rulesOwner`、`hasRules`、各规则字段长度、`ruleTag.len`。
- `[LoginSync][integrity.cloud_model_catalog]` — 云端目录回调：`catalogOwner`、`rawJson.len`、`hasModelCatalog`、`defaultPriority.effective.len`、`defaultTextP1`、`defaultPriority.preview180`（与 `model_catalog_get` 互补）。
- `[LoginSync][integrity.project_rules_load_failed]` / `[integrity.model_catalog_load_failed]` — 拉取失败：`code`、`msg`、`requestedOwner`（目录失败时）。
- `[LoginSync][login.provider_keys.plan]` / `login.provider_keys.dispatch` / `dispatch_skip` / `plan_skip` / `fail` — 登录后密钥拉取计划与实际请求（含 `ownerUserIdLen`、跳过原因：`local_db_has_key` / `inflight` 等）。
- `[LoginSync][login.settings.pull_plan]` — **总计划**：`projectRulesPullOwner`、`modelSettingsCloudOwnersSeq`（按序发起的 `model_settings_get` 归属用户名，`|` 分隔）；说明图 1「默认配置」来自 `defaultPriorityJson` 等落库。
- `[LoginSync][login.project_rules.dispatch]` / `dispatch_skip` — 项目规则下载请求（含 `localRulesNonEmpty`）。
- `[LoginSync][login.model_settings.dispatch]` / `dispatch_skip` — 模型设置下载请求（含 `localModelSettingsNonEmpty`；与图 1 默认模型优先级相关）。
- `[LoginSync][super_chain.attempt]` — 超级用户链回退时当前 `pullOwner` 与链索引。
- 既有：`login.project_rules.persist_ok|fail`、`login.model_settings.cloud_ok|persist|fail`；`persist` 行增补 `defaultPriorityPreview`（前 140 字，仅供核对是否为空/截断）。
- **Step 7 jobs 云同步**：`[LoginSync][login.generation_jobs.dispatch]`（`phase=first|next`）、`[LoginSync][login.generation_jobs.merge]`、`[LoginSync][login.generation_jobs.chain_done]`、`[LoginSync][login.generation_jobs.fail]`、`[LoginSync][generation_jobs.cloud_push.dispatch]`（终态 / billing 后增量推 DTO）；`[LoginSync][generation_jobs.cloud_push.active_batch]` / `active_skip`（登录 pull 合并结束或 pull 失败后，批量 upsert 本地 `running`/`queued`/`pending`/`processing`/`in_progress` jobs）。

**C++（`GenerationJobRepository.cpp`，`qWarning().noquote()`）**（Step 7 合并上下文）：

- `[ISO][GenerationJobRepository][context.patch]` — `jobId`、`ok`、`keys`（合并进 `jobs.context_json` 的键名列表，**不含**积分密钥）。
- `[ISO][GenerationJobRepository][artifact.resolve]` — `resourceId` 查库：`no_storage` / `not_found`；成功时 `ok=1`、`jobId`、`rid_len`、`primary_len`、`thumb_len`（Step 12 双路径解析）。
- `[ISO][GenerationJobRepository][artifact.download.begin|fail|end|abort]` — Step 13 终态成功后异步 GET 远程产物：按 `jobId`/`ordinal`、`url_len`、`bytes`、`patch`（写回本地 `uri`/`thumbnail_uri`，**不覆盖** `remote_uri`）。

**C++（`CloudAuthService.cpp`，`qWarning().noquote()`）**：

- `[CloudAuth][list_users.begin]` — `token_len`、`cloud_enabled`、`gateway`。
- `[CloudAuth][model_settings_get.payload_keys]` — 云端 `data` 对象键名列表。
- `[CloudAuth][model_settings_get.default_priority_preview]` — `defaultPriorityJson` 前 180 字符预览（无换行）；**不含** API Key。
- `[CloudAuth][model_catalog_get.display_emit]` — `owner`、`hasModelCatalog`、`jsonLen`、`json_utf8_bytes`、`hashLen`、`schema`、`catVer`。
- `[CloudAuth][model_catalog_get.payload_large]` — 目录 UTF-8 ≥ 4MiB 时追加：`json_utf8_bytes`、`warn_threshold_utf8`、`server_max_utf8`（8MiB，与云端写入上限对齐）。
- `[CloudAuth][model_catalog_set.reject_local]` — 客户端拒绝超 8MiB 的上传：`utf8_bytes`、`max`。

---

## 10.1 积分日志 · 完成时间链路（`completedAt` → 本地 `completed_time`）

排查「完成时间一直为 `-`」时，在 **Qt Creator → Application Output** 按顺序看（UTC ISO 时间戳在 `[CloudAuth]` / `[PointsLogStorage]` 行首方括号内）：

**C++ `CloudAuthService.cpp`（`qWarning`）**

- **`points_ledger_list` 成功路径**（`dispatch` / `ok` / `row_sample` / `user_max_time` 等）：**仅当** `AIMODEL_VERBOSE_LOG` 开启时输出；**失败路径** `points_ledger_list.fail` 仍默认输出以便发现问题。
- `[ISO][CloudAuth][points_ledger_list.row_sample]`（verbose）— 云端 JSON **前 5 行**：`logId`、`completedAt` / `completed_at` 的**类型**（`string(len=…)` / `object:{…}` / `undefined` 等）、`logTime_len`。若出现 **`object:`** 而**没有** `string`，说明 DB 把日期存成了非字符串，QML `_pickText` 会得到空。

**C++ `PointsLogStorage.cpp`（`qWarning`）**

- `[ISO][PointsLogStorage][save.ui_row]` — 写入前来自 QML 的 `timeCompleted` / `completedTime` 字符串长度、打包后 `completed` 长度（前 5 行，需 `AIMODEL_VERBOSE_LOG`）。
- `[ISO][PointsLogStorage][load.page_row]` — 分页读出后 `timeCompleted` / `timeCreated` 长度（前 5 行，需 verbose）。
- **`[ISO][PointsLogStorage][save.ui_ingest]`**（需 **`AIMODEL_VERBOSE_LOG`**）— 云端落库前：`inputRows`、`distinct_userName`、前 16 个 `userName` 样本（`|` 拼接），用于核对是否混入多用户。
- **`[ISO][PointsLogStorage][load.page_scope]`**（需 verbose）— 当前分页 SQL 条件摘要：`userNameExact`、`off`/`lim`、时间/原因/模型筛选长度、`row_after_dedupe`。列表为筛选条件下 **全局** `ORDER BY log_time DESC, id DESC` 再分页，**不按用户截断**；首屏若几乎同一用户名，多为该用户在当前窗内时间最新记录集中在前。
- **`[ISO][PointsLogStorage][load.page_users]`**（需 verbose）— 本页**排序后**行内的 `userName`：`distinct_in_page`、`order_pipe`（`|` 按展示顺序）。若「统计行有多用户、表格首屏只有一人」，优先核对此处是否为**按 `log_time` 倒序取顶页**导致，而非未拉云。
- **`[ISO][PointsLogStorage][aggregate.by_user]`**（需 verbose）— 与当前筛选一致的汇总：`rowCount`、`groupCount`（GROUP BY 用户数）、`users_pipe`。

**QML `PointsLogPage.qml`（`console.warn` → `PointsLogDebug`）**

- `[*][PointsLogDebug][cloud.normalize]` — 归一化前 5 行（需 `aimodelVerboseLog`）：`completedAt` 等。
- `[*][PointsLogDebug][db.load.before_display]` — 送进 `_toDisplayLogRow` 前（需 verbose）。
- **`[*][PointsLogDebug][audit]`**（需根上下文 **`aimodelVerboseLog`**，与 `AIMODEL_VERBOSE_LOG` 一致）— `query.begin`（`super`/`selfOnlyRole`/筛选）、`cloud.http_payload`（回调包内 `userName` 去重数与样本）、`db.display_page`（当前页展示用名列表）。用于与 C++ `load.page_users` 对照。

**说明**：`PointsLogStorage` 内历史函数 `pointsBalanceLog` 仍为空实现；积分日志详细排障请先设 **`AIMODEL_VERBOSE_LOG=1`**（或等价），再按本节 tag 过滤。

---

## 10.1.1 任务表云端读入 · 本地 `generation_tasks_ext` merge（`generation_tasks_table_list`）

用于区分 **HTTP/业务读云端失败** 与 **SQLite 写本地失败**。**失败路径**默认 `qWarning` 可见；**成功路径**（`response` / `kind_probe` / `ok` / `row_sample`、`merge.begin` / `merge.end`）仅在 **`AIMODEL_VERBOSE_LOG=1`**（`aimodelVerboseLogEnabled()`）时输出。

**判定顺序**

1. **云端读失败**：先出现 `[CloudAuth][generation_tasks_table_list.network_error|biz_fail]`，且带 **`layer=cloud_read_fail`** → 未到本地 merge，或 `rows` 为空且无成功 `response`。
2. **云端读成功（verbose）**：出现 `[CloudAuth][generation_tasks_table_list.response]`，且 **`layer=cloud_read_ok`**；前 5 行 **`kind_probe`** 说明云端 JSON 是否含 `kind`（`absent` / `str_empty` / `str_len=N`）。
3. **本地写失败**：**`[task_ext.merge.row_fail]`**（最多 32 条）与 **`[StoryboardStorage][task_ext.upsert_fail]`**（`layer=local_sql`，含 **`had_local_row` / `existing_kind_sql_null`**，用于区分 INSERT vs 旧行 UPDATE 路径）。成功汇总 **`merge.end`** 仅 verbose。

**C++（`CloudAuthService.cpp`）**

- `[ISO][CloudAuth][generation_tasks_table_list.response] layer=cloud_read_ok …` — **verbose**：每页成功解析后一条。
- `[ISO][CloudAuth][generation_tasks_table_list.kind_probe] …` — **verbose**。
- `[ISO][CloudAuth][generation_tasks_table_list.network_error|biz_fail] layer=cloud_read_fail …` — 读失败（**默认可见**）。

**C++（`GenerationJobRepository.cpp`）**

- `[ISO][GenerationJobRepository][task_ext.merge.begin] …` — **verbose**。
- `[ISO][GenerationJobRepository][task_ext.merge.row_fail] layer=local_sql_upsert …` — 单条 upsert 失败样本（最多 32 条）（**默认可见**）。
- `[ISO][GenerationJobRepository][task_ext.merge.end] …` — **verbose**。

**C++（`StoryboardStorage.cpp`）**

- `[ISO][StoryboardStorage][task_ext.upsert_fail] layer=local_sql … had_local_row=… existing_kind_sql_null=… err=…`

**QML（`main.qml`，`TaskExtCloudDebug`）**

- `[*][TaskExtCloudDebug] layer=qml_merge_callback … diagnose=…` — QML 收到 `generationJobsCloudLoaded` 后合并结果摘要（与 C++ 对照：`errors` 来自 `applyCloudTaskExtRows`）。

---

## 10.2 实体「选择结果图」/ 工作台上传（`MatchImagePopup` / `jobs` ingest）

排查「弹窗全是失败占位 / 上传后不显示 / 人物结果图不对」时，在 **Qt Creator → Application Output** 过滤下列 tag（时间在首段方括号内为 UTC ISO）。

**QML（`ProjectPage.qml`，`console.warn`）**

- **`EntityMatchImageDebug`**（模块名固定，便于过滤）
  - `[EntityMatchImageDebug][history.load.begin|end]` — 拉实体行历史：`wantTag`（`character_N` 等）、`pk`/`legacy` 长度、`entries_len`、`merged_pre_dedupe`。
  - `[EntityMatchImageDebug][history.job]` — 每条命中任务的归一化结果：`phase=` `merged` / `dropped_null_normalize` / `dropped_tag_mismatch`；`jobId`、`job_status`、`ctx.source`（如 `workbench_user_entity_result`）、`ctx.taskType`、`out_sizeTag`、`out_provider`、`isErrEntry`、`imageSource_prefix`（截断）。
  - `[EntityMatchImageDebug][apply.result.begin|done|ingest_fail|abort …]` — 工作台拖入/上传写入 `jobs` 的 QML 路径：`scheme=` `file|http|path_or_other`、`rid_len`、失败原因。
  - `[EntityMatchImageDebug][pool.skip_error_shape]` — 行内 `resultImages` 某段被判定为错误占位形状（svg/data）而未进成功网格：`prefix` 截断。
  - `[EntityMatchImageDebug][popup.merge_jobs]` — 网格为空时从 `queryJobs` 合并的成功态：`appended`。
  - `[EntityMatchImageDebug][popup.confirm]` — 确认选用：`rid_sel_len`、`rid_resolved_len`、`lookup_used`、`promote_ret`。
- **`MatchImagePopupDebug`**（沿用既有）
  - `[MatchImagePopupDebug][open]` — 打开弹窗：`projectKey_len`、`legacyKey_len`。
  - `[MatchImagePopupDebug][list]` — 刷新列表：`success_cat_cells`、`cat_arr_len`、`resImg_poolLooksErr`、`err_cards`、`jobs_injected` 等。
  - `[MatchImagePopupDebug][jobs.begin|jobs.match|jobs.end|row.skip|jobs.skip]` — 与任务池匹配行一致。

**C++（`qWarning().noquote()`）**

- **`[ISO][StoryboardStorage][workbench.ingest]`** — `begin`（`artifact_clip`、`mime`）、`done`（`jobId`、`rid_len`、`entity`、`row`）、`abort reason=*`（缺文件、无 artifact、`upsert` 失败等）。
- **`[ISO][GenerationJobRepository][workbench.ingest]`** — `ok` / `fail`（`insert_failed`、`no_storage`）；与 `entity.image.pick`（`StoryboardStorage][entity.image.pick` **hit/miss**）可串联。

**说明**：与既有 `[EntityImageDebug][primary.map…]`（工作台主图槽位）一起看，可区分「库已写入 / 行内 URL / 弹窗列表 / 主图查询」哪一段断裂。

---

## 11. 规则文件查看回执（强制）

- 当回答中提到“已查看规则文件 / 按规则执行”时，必须明确列出**已查看规则文件名**。
- 文件名必须写全名并带扩展名（例如：`debug-logging.mdc`）。
- 若查看多个规则文件，必须逐条列出每个文件名。
- 禁止只写“已看规则”但不写文件名。
